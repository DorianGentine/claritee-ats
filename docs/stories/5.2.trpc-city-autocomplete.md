# Story 5.2: Procédure tRPC city.autocomplete

## Status

TODO

## Story

**As a** developer,
**I want** a `city.autocomplete` tRPC procedure,
**so that** the frontend can search cities with local-first, Photon fallback logic.

## Acceptance Criteria

1. Router `city` créé (`src/server/trpc/routers/city.ts`) et enregistré dans `_app.ts`
2. Procédure `city.autocomplete` : `publicProcedure`, input `{ q: string }`, output `{ id, name, region, country }[]`
3. Si `q.length < 3` → retourne `[]` immédiatement sans requête DB
4. Recherche locale : `$queryRaw` avec `unaccent(name) ILIKE unaccent($1) || '%'`, `ORDER BY name ASC`, `LIMIT 5`
5. Si résultats locaux → retourne sans appel externe
6. Si aucun résultat local → appel Photon (`lang=fr`, `limit=5`, `layer=city`, `bbox=-10,35,40,72`)
7. Résultats Photon non présents en DB → créés à la volée via `upsert` sur `(name, country)`
8. Si Photon inaccessible (timeout ou erreur réseau) → `console.error` + retourne `[]` sans exception côté client
9. Coordonnées `latitude`/`longitude` non exposées dans l'output
10. Tests unitaires : q < 3 → [] ; DB locale trouvée → retour direct ; DB vide + Photon mocké → upsert + retour ; DB vide + Photon KO → []

## Tasks / Subtasks

- [ ] Task 0 — Pré-requis
  - [ ] Vérifier que la story 5.1 est DONE (table `cities` en base, extensions actives)
  - [ ] Lire `src/server/trpc/routers/_app.ts` (pour savoir comment enregistrer le nouveau router)
  - [ ] Lire `docs/architecture/adr/0005-villes-structurees-table-city-api-photon.md` §4–6

- [ ] Task 1 (AC: 1) — Créer le router `city`
  - [ ] Créer `src/server/trpc/routers/city.ts` avec la structure de base (template standard du projet)
  - [ ] Enregistrer dans `src/server/trpc/routers/_app.ts` : `city: cityRouter`

- [ ] Task 2 (AC: 2–5) — Implémenter la recherche locale
  - [ ] Créer la procédure `city.autocomplete` avec `publicProcedure`
  - [ ] Input Zod : `z.object({ q: z.string() })`
  - [ ] Guard : si `input.q.trim().length < 3` → `return []`
  - [ ] Requête `$queryRaw` :
    ```sql
    SELECT id, name, region, country
    FROM cities
    WHERE unaccent(name) ILIKE unaccent($1) || '%'
    ORDER BY name ASC
    LIMIT 5
    ```
  - [ ] Si `localResults.length > 0` → retourner directement

- [ ] Task 3 (AC: 6–8) — Implémenter le fallback Photon
  - [ ] Si aucun résultat local → appel `fetch` vers Photon :
    ```
    https://photon.komoot.io/api/?q={q}&lang=fr&limit=5&layer=city&bbox=-10,35,40,72
    ```
  - [ ] Parser la réponse GeoJSON : `features[].properties` (name, state→region, country) et `features[].geometry.coordinates` ([longitude, latitude])
  - [ ] `upsert` Prisma pour chaque résultat Photon :
    ```ts
    prisma.city.upsert({
      where: { name_country: { name, country } },
      update: {},
      create: { name, region, country, latitude, longitude },
    })
    ```
    (nécessite `@@unique([name, country])` sur le modèle `City` — à ajouter en 5.1 si oublié)
  - [ ] Entourer le bloc Photon dans un `try/catch` : en cas d'erreur → `console.error(...)` + retourner `[]`
  - [ ] Ajouter `@@unique([name, country])` sur le modèle `City` dans `schema.prisma` si non présent

- [ ] Task 4 (AC: 10) — Tests unitaires
  - [ ] Créer `tests/unit/routers/city.autocomplete.test.ts`
  - [ ] Cas 1 : q = "Pa" (< 3 chars) → retourne []
  - [ ] Cas 2 : q = "Par", DB retourne [Paris] → retourne [Paris] sans appel Photon
  - [ ] Cas 3 : q = "Bra", DB vide, Photon mocké → upsert appelé + retourne [Bratislava]
  - [ ] Cas 4 : q = "Xyz", DB vide, Photon lance une exception → retourne []

## Dev Notes

### Previous Story Insights

- Story 5.1 terminée : table `cities` créée, extensions `unaccent` + `pg_trgm` actives, index GIN en place, seed exécuté
- Aucun router `city` n'existait avant cette story

### API Specifications

**Photon API :**
```
GET https://photon.komoot.io/api/?q={q}&lang=fr&limit=5&layer=city&bbox=-10,35,40,72
```
Réponse :
```json
{
  "features": [{
    "properties": { "name": "Lyon", "state": "Auvergne-Rhône-Alpes", "country": "France" },
    "geometry": { "coordinates": [4.8357, 45.7640] }  // [longitude, latitude]
  }]
}
```
Note : `geometry.coordinates[0]` = longitude, `geometry.coordinates[1]` = latitude (ordre GeoJSON).

**Contrainte `@@unique([name, country])` sur `City` :**  
Nécessaire pour l'upsert Photon. Si oublié dans 5.1, ajouter en début de cette story et générer une migration additive.

**`publicProcedure` :** les villes sont des données de référence non sensibles — pas besoin d'authentification pour l'autocomplétion. [Source: ADR 0005 §6]

### File Locations

| Fichier | Action |
|---------|--------|
| `src/server/trpc/routers/city.ts` | Créé |
| `src/server/trpc/routers/_app.ts` | Modifié (ajout `city: cityRouter`) |
| `src/lib/validations/city.ts` | Créé (schema Zod input `{ q: z.string() }`) |
| `tests/unit/routers/city.autocomplete.test.ts` | Créé |

[Source: docs/architecture/source-tree.md]

### Technical Constraints

- `$queryRaw` retourne des objets avec types raw Postgres (Decimal, etc.) — caster `latitude`/`longitude` en Number si besoin mais ils ne sont pas dans l'output
- Ne pas utiliser `findMany` avec `contains` pour cette recherche : elle ne tire pas parti de l'index GIN `unaccent`. Utiliser `$queryRaw`
- Timeout Photon : utiliser `AbortSignal.timeout(3000)` dans le `fetch` pour éviter de bloquer la procédure indéfiniment
- `console.log` interdit (ESLint warn) — utiliser `console.error` pour les erreurs Photon

### Testing

- Mock Prisma `$queryRaw` pour les tests unitaires
- Mock `fetch` global pour les appels Photon
- Tests dans `tests/unit/routers/` conformément à `docs/architecture/14-stratgie-de-tests.md`

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-14 | 0.1 | Story draft créée | John (PM) |

---

## Dev Agent Record

*(À remplir par l'agent de développement)*

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

---

## QA Results

*(À remplir par l'agent QA)*
