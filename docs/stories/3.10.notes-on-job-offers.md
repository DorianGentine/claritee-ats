# Story 3.10: Notes on Job Offers

## Status

READY

## Story

**As a** recruiter,  
**I want** to add notes on job offers,  
**so that** I can document client requirements and updates.

## Acceptance Criteria

1. La fiche offre `/offers/[id]` comporte une **section Notes** dédiée, visible pour tous les utilisateurs du cabinet  
2. Le système de notes réutilise le **même modèle et la même logique** que pour les notes sur candidats (Story 3.9), en attachant cette fois les notes à une offre (champ `offerId`)  
3. Un champ ou bouton **"Ajouter une note"** permet de créer une nouvelle note sur l’offre directement depuis la fiche offre  
4. Le contenu de la note est saisi via un **éditeur riche BlockNote** (même système que pour les notes candidats : blocs texte, listes, titres — inspiration Notion)  
5. Chaque note stocke les métadonnées suivantes : **auteur** (utilisateur), `createdAt` (date/heure de création)  
6. Les notes sont stockées et affichées sur la fiche offre en **flux type chat**, comme sur les fiches candidats (ordre chronologique naturel en lecture, basé sur `createdAt asc`)  
7. Un utilisateur peut **éditer uniquement ses propres notes** (pas celles des autres utilisateurs)  
8. Un utilisateur peut **supprimer ses propres notes** avec confirmation (dialogue de confirmation avant suppression)  
9. Les notes sont **partagées au niveau du cabinet** : toutes les notes d’une offre sont visibles par tous les utilisateurs de la même `companyId`  
10. Si aucune note n’existe pour une offre, la section affiche un **état vide** clair (ex. "Aucune note") avec un call-to-action vers la création d’une première note

**Réf.** PRD § Epic 3, Story 3.10 ; Architecture §5 (Note), §9 (router `note`) ; Design System §2 (typographie) ; Wireframes §5 section Notes (fiche offre).

> Note :  
> - Cette story réutilise le modèle `Note` introduit pour les **notes sur candidats** (Story 3.9), en ajoutant la vue et les actions côté offre.  
> - Le widget de note rapide (Story 3.11) et la page "Mes notes" (Story 3.12) réutiliseront également ce modèle mais ne sont pas dans le périmètre ici.

## Tasks / Subtasks

- [ ] Task 0 – Standards & architecture (pré‑requis)  
  - [ ] Avant de commencer l’implémentation de cette story, lire (ou relire) les documents suivants et s’assurer que les décisions de code et de test y sont conformes :
    - `docs/architecture/coding-standards.md`
    - `docs/frontend-architecture.md`
    - `docs/architecture.md`
  - [ ] Adapter au besoin les tâches ci‑dessous pour respecter les patterns recommandés (structure des routers tRPC, conventions frontend, stratégie de tests, a11y, DRY, etc.).

- [ ] Task 1 (AC: 2, 4, 5, 9) – Modèle & API Note (côté offre)

  - [ ] Vérifier que le modèle Prisma `Note` dans `prisma/schema.prisma` couvre bien les deux cas :
    - [ ] Note liée à un **candidat** (champ `candidateId` non nul, `offerId` null).  
    - [ ] Note liée à une **offre** (champ `offerId` non nul, `candidateId` null).  
  - [ ] Champs minimaux requis (rappel PRD) : `id`, `content`, `authorId`, `candidateId` (nullable), `offerId` (nullable), `companyId`, `createdAt`. (`updatedAt` peut être présent même s’il n’est pas mentionné dans le PRD, utile pour l’édition).  
  - [ ] Vérifier que `companyId` est correctement renseigné et que le modèle est scopé par tenant (multi‑tenancy).  
  - [ ] Dans le router tRPC `note` (ex. `src/server/trpc/routers/note.ts`) ou équivalent :
    - [ ] Ajouter / vérifier une procédure pour **lister les notes par offre**, ex. `note.listByOffer` :
      - [ ] Input Zod : `{ offerId: z.uuid() }`.  
      - [ ] `protectedProcedure` filtrant par `companyId` et par `offerId`.  
      - [ ] Tri par `createdAt` descendant pour l’ordre anté-chronologique.  
    - [ ] Ajouter / vérifier une procédure pour **créer une note sur une offre**, ex. `note.createForOffer` :
      - [ ] Input Zod : `{ offerId: z.uuid(), content: z.string().min(1) }`.  
      - [ ] `protectedProcedure` créant une note avec `offerId`, `candidateId = null`, `authorId = ctx.user.id`, `companyId = ctx.companyId`, où `content` est la **chaîne JSON du document BlockNote**, exactement comme pour les notes candidats.  
    - [ ] Ajouter / vérifier des procédures communes `note.update` / `note.delete` ou dédiées qui :
      - [ ] Vérifient que la note appartient au `companyId` courant.  
      - [ ] Vérifient que seul l’**auteur** (`authorId`) peut éditer ou supprimer la note.

- [ ] Task 2 (AC: 1, 3, 4, 5, 6, 10) – UI section Notes sur la fiche offre

  - [ ] Créer ou compléter un composant de section Notes pour la fiche offre, par ex. `OfferNotesSection` dans `src/components/offers/OfferNotesSection.tsx` :
    - [ ] Le composant reçoit l’`offerId` et/ou la liste des notes récupérées via tRPC.  
    - [ ] Il affiche un **header** clair (ex. "Notes") avec éventuellement un compteur de notes.  
  - [ ] Intégration dans `OfferDetailView` (Story 3.6) :
    - [ ] Charger les notes de l’offre via `note.listByOffer` avec TanStack Query.  
    - [ ] Gérer les états `loading` (skeleton ou spinner), `error` (message générique, possibilité de réessayer) et `success`.  
  - [ ] Affichage des notes :
    - [ ] Liste triée par `createdAt` ascendant (ordre chrono, les plus anciennes en premier) et rendue comme un **flux type chat** (bulles ou blocs empilés), exactement comme pour les notes candidats (Story 3.9).  
    - [ ] Pour chaque note, afficher :
      - [ ] Le contenu rendu via le composant BlockNote en lecture seule à partir de la chaîne JSON `content` (même composant que pour les notes candidats, idéalement factorisé).  
      - [ ] Le nom de l’auteur (dérivé de `authorId` → `User` ou pré‑joint).  
      - [ ] La date de création formatée (`createdAt`) de manière cohérente (ex. "12 mars 2026, 09:15").  
  - [ ] Ajout d’une action **"Ajouter une note"** :
    - [ ] Champ inline ou bouton ouvrant un petit formulaire (texte + bouton "Enregistrer").  
    - [ ] Appel de la mutation `note.createForOffer`.  
    - [ ] Sur succès : vider le champ, invalider ou mettre à jour la query des notes, afficher un `toast.success`.  
    - [ ] Sur erreur : afficher un `toast.error` générique.  
  - [ ] État vide :
    - [ ] Si aucune note n’existe, afficher un message "Aucune note" + un call-to-action (bouton ou lien "Ajouter une note") cohérent avec le PRD.

- [ ] Task 3 (AC: 7, 8, 9) – Édition et suppression des notes (permissions auteur)

  - [ ] Pour chaque note, ajouter des actions **Éditer** et **Supprimer** visibles uniquement si la note appartient à l’utilisateur courant (`authorId === ctx.user.id` côté serveur, reflet côté front).  
  - [ ] Édition :
    - [ ] Permettre d’entrer dans un mode édition (inline ou modal) avec le contenu texte de la note.  
    - [ ] Appeler la mutation `note.update` avec les contrôles suivants :
      - [ ] Seul l’auteur peut éditer (`authorId` + `companyId` vérifiés côté back).  
      - [ ] `content` ne doit pas être vide.  
    - [ ] Sur succès : mettre à jour la note dans la liste (ou invalider la query), afficher un `toast.success`.  
    - [ ] Sur erreur : revert au contenu précédent, afficher un `toast.error` générique.  
  - [ ] Suppression :
    - [ ] Ajouter une action "Supprimer" ouvrant un **dialogue de confirmation** ("Voulez-vous vraiment supprimer cette note ?").  
    - [ ] Sur confirmation, appeler `note.delete` :
      - [ ] Vérifier côté back que seul l’auteur peut supprimer.  
    - [ ] Sur succès : retirer la note de la liste, afficher un `toast.success`.  
    - [ ] Sur erreur : afficher un `toast.error`.  
  - [ ] Vérifier que les notes supprimées n’apparaissent plus ni sur la fiche offre ni dans d’autres écrans (ex. futures Stories 3.11/3.12).

- [ ] Task 4 – Intégration & cohérence avec Story 3.9

  - [ ] S’assurer que le **router `note`** et le modèle `Note` restent **DRY** et mutualisés entre :  
    - [ ] Notes sur candidats (Story 3.9).  
    - [ ] Notes sur offres (Story 3.10).  
    - [ ] Futures notes libres / widget (Stories 3.11–3.12).  
  - [ ] Vérifier que les changements apportés pour l’offre ne cassent pas la fiche candidat (réutilisation des mêmes procédures et types).  
  - [ ] Harmoniser le style visuel des notes entre fiche candidat et fiche offre (typographie, espacements, état vide, CTA).

- [ ] Task 5 – Testing (AC: 1–10)

  - [ ] Tests d’intégration back (Vitest) :
    - [ ] `note.listByOffer` :
      - [ ] Retourne uniquement les notes de l’offre du `companyId` courant, triées par `createdAt` desc.  
      - [ ] Ne retourne jamais de notes d’un autre cabinet.  
    - [ ] `note.createForOffer` :
      - [ ] Création d’une note valide (offer + user du même `companyId`, contenu non vide).  
      - [ ] Erreur si l’offre n’appartient pas au `companyId` courant.  
    - [ ] `note.update` / `note.delete` :
      - [ ] Succès lorsque l’utilisateur courant est l’auteur.  
      - [ ] Erreur / UNAUTHORIZED si un utilisateur tente d’éditer/supprimer la note d’un autre.  
  - [ ] Tests unitaires :
    - [ ] Schémas Zod d’input pour les procédures `note.listByOffer`, `note.createForOffer`, `note.update`, `note.delete`.  
  - [ ] Tests de composants (au moins smoke tests) :
    - [ ] `OfferNotesSection` :
      - [ ] Affichage des notes dans le bon ordre.  
      - [ ] Affichage de l’état vide "Aucune note".  
      - [ ] Flux de création de note (mock de mutation).  
      - [ ] Flux d’édition/suppression conditionnés par le fait que la note soit celle de l’utilisateur courant (peut être simulé par props).  

- [ ] Task 6 (QA) – Cas de test

  - [ ] Rédiger les cas de test QA détaillés dans un fichier dédié `docs/stories/cases/3.10-test-cases.md` (scénarios : création simple, édition, suppression, permissions auteur, multi‑tenancy, cohérence entre fiche candidat et offre, état vide) pour faciliter la review de la story.

- [ ] Task 7 (review) – Code review

  - [ ] Lancer une review de code en fin de développement avant clôture de la story (revue du modèle/Router `note`, de la section Notes de la fiche offre, des permissions auteur et des tests).

## Dev Notes

### Dependencies & Previous Stories

- **Story 3.6** : fiche offre détaillée `/offers/[id]` (structure de la page, sections header / détails / candidats / tags).  
- **Story 3.9** : notes sur candidats (modèle `Note`, router `note`, section Notes sur la fiche candidat, éditeur riche BlockNote).  
- Cette Story 3.10 étend l’usage du modèle `Note` aux **offres**, en gardant un contenu texte simple côté offre, tout en partageant l’API et les règles de permissions.

### Data Models

- **Note** (rappel / complétion) :  
  - `id` (UUID)  
  - `content` (texte ; pour les offres, plain text)  
  - `authorId` (FK `User`)  
  - `candidateId` (FK `Candidate`, nullable)  
  - `offerId` (FK `JobOffer`, nullable)  
  - `companyId` (FK `Company`)  
  - `createdAt`, `updatedAt`  
- Invariants pour cette story :
  - Pour une note d’offre : `offerId` non nul, `candidateId` null.  
  - Les notes sont toujours scopées par `companyId`.

### API Specifications

- **note.listByOffer** :
  - Input : `{ offerId: z.uuid() }`.  
  - Comportement :
    - Vérifier que l’offre appartient au `companyId` courant.  
    - Retourner toutes les notes associées, triées par `createdAt` asc (flux type chat ; le composant front peut inverser l’affichage ou utiliser un scroll adapté si nécessaire).  
- **note.createForOffer** :
  - Input : `{ offerId: z.uuid(), content: z.string().min(1) }`.  
  - Comportement :
    - Vérifier `companyId`.  
    - Créer une note avec `offerId`, `candidateId = null`, `authorId = ctx.user.id`, `companyId = ctx.companyId`.  
- **note.update** / **note.delete** :
  - Contrôler que `authorId === ctx.user.id` et que `companyId` correspond ; sinon renvoyer une erreur UNAUTHORIZED/NOT_FOUND.

### UI & Components

- **OfferNotesSection** :
  - Doit respecter le layout et les spacing de la fiche offre.  
  - Le pattern d’édition/suppression doit être cohérent avec celui de la section Notes de la fiche candidat (Story 3.9).  
- Penser à l’extensibilité future pour le widget 3.11 et la page "Mes notes" 3.12 (ex. factoriser les composants d’affichage d’une note individuelle).

### Testing

- Prioriser les scénarios critiques :
  - Création / édition / suppression de notes sur une offre par l’auteur.  
  - Accès en lecture pour tous les utilisateurs du même cabinet, interdiction pour un autre cabinet (multi‑tenancy).  
  - Comportement en absence de notes (état vide).  
- Vérifier la cohérence visuelle et UX entre fiche offre et fiche candidat.

---

## Change Log

| Date       | Version | Description                                          | Author |
|------------|---------|------------------------------------------------------|--------|
| 2026-03-11 | 0.1     | Story draft created (3.10 Notes on Job Offers)      | Bob (SM) ou équivalent |

---

## Dev Agent Record

### Agent Model Used

(Générer / compléter lors de l’implémentation réelle de la story.)

### Debug Log References

*(Aucune pour le draft.)*

### Completion Notes List

*(À compléter par l’agent de dev lors de la mise en œuvre.)*

### File List

*(À compléter par l’agent de dev lors de la mise en œuvre : routers tRPC, composants, tests, docs QA, etc.)*

---

## QA Results

*(À remplir par l’agent QA après review de la story implémentée.)*

