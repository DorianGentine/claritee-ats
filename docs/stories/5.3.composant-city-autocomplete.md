# Story 5.3: Composant partagé CityAutocomplete

## Status

done

## Story

**As a** developer,
**I want** a shared `CityAutocomplete` component,
**so that** all forms can pick cities consistently without duplicating logic.

## Acceptance Criteria

1. Composant `src/components/shared/CityAutocomplete.tsx` créé
2. Props : `value: CityOption | CityOption[] | null`, `onChange`, `mode: "single" | "multi"`, `placeholder?`, `disabled?`  
   (`CityOption = { id: string; name: string; region?: string | null; country: string }`)
3. Basé sur `Popover` + `Command` (shadcn Combobox pattern), debounce 300ms
4. Minimum 3 caractères pour déclencher la recherche — affiche "Saisir au moins 3 caractères" sinon
5. État chargement pendant la query (`isLoading` → spinner ou "Recherche...")
6. `mode="single"` : ville sélectionnée affichée comme badge clearable ; clic sur le corps du badge → ouvre le popover pour changer de ville ; clic sur le × du badge → efface la sélection
7. `mode="multi"` : chaque ville sélectionnée apparaît comme chip ordonné avec boutons ↑, ↓ et × ; ↑ désactivé sur index=0, ↓ désactivé sur le dernier
8. Chaque option de liste : nom de la ville en texte principal + `Région · Pays` en `text-muted-foreground` dessous
9. Ville déjà sélectionnée : absente des suggestions (filtre côté client)
10. État vide : "Aucune ville trouvée"
11. Tests unitaires : mode single sélection + clear ; mode multi ajout + suppression + ↑ + ↓

## Tasks / Subtasks

- [x] Task 0 — Pré-requis
  - [x] Vérifier que la story 5.2 est DONE (procédure `city.autocomplete` disponible)
  - [x] Lire `src/components/shared/` pour connaître les composants partagés existants
  - [x] Consulter `docs/design-system.md` (palette, shadcn, typographie)
  - [x] Vérifier que `useDebounce` existe dans le projet — sinon l'implémenter ou installer `use-debounce`

- [x] Task 1 (AC: 1–3) — Créer la structure du composant
  - [x] Créer `src/components/shared/CityAutocomplete.tsx`
  - [x] Définir le type `CityOption = { id: string; name: string; region?: string | null; country: string }`
  - [x] Définir les props avec union sur `mode` :
    ```ts
    type Props =
      | { mode: "single"; value: CityOption | null; onChange: (city: CityOption | null) => void; placeholder?: string; disabled?: boolean }
      | { mode: "multi"; value: CityOption[]; onChange: (cities: CityOption[]) => void; placeholder?: string; disabled?: boolean }
    ```
  - [x] Implémenter le debounce sur le champ de saisie (300ms)
  - [x] Appeler `api.city.autocomplete.useQuery({ q: debouncedQuery }, { enabled: debouncedQuery.length >= 3 })`

- [x] Task 2 (AC: 4–5) — Popover + Command (liste de suggestions)
  - [x] Utiliser `<Popover>` + `<PopoverTrigger>` + `<PopoverContent>` (shadcn)
  - [x] Intérieur du Popover : `<Command>` + `<CommandInput>` (affiche la query en cours) + `<CommandList>`
  - [x] Si `debouncedQuery.length < 3` → `<CommandEmpty>Saisir au moins 3 caractères</CommandEmpty>`
  - [x] Si `isLoading` → `<CommandEmpty>Recherche...</CommandEmpty>`
  - [x] Suggestions filtrées (sans les villes déjà sélectionnées) : `<CommandItem>` pour chaque résultat
  - [x] Chaque `<CommandItem>` : ligne principale = `city.name`, ligne secondaire = `city.region · city.country` (muted)

- [x] Task 3 (AC: 6) — Mode single
  - [x] Trigger du Popover : badge terracotta affichant le nom de la ville sélectionnée + bouton × ; ou bouton "Sélectionner une ville" si null
  - [x] Clic sur une suggestion : appelle `onChange(city)` et ferme le Popover
  - [x] Clic sur × du badge : appelle `onChange(null)`

- [x] Task 4 (AC: 7) — Mode multi (chips + ↑↓)
  - [x] Liste des chips sélectionnés affichée **au-dessus** du trigger Popover
  - [x] Chaque chip : nom de la ville + bouton ↑ + bouton ↓ + bouton ×
  - [x] ↑ : swapper la ville avec celle d'index − 1 → `onChange([...reordered])`
  - [x] ↓ : swapper la ville avec celle d'index + 1 → `onChange([...reordered])`
  - [x] ↑ sur index=0 : désactivé (`disabled`)
  - [x] ↓ sur dernier index : désactivé (`disabled`)
  - [x] × : supprimer la ville du tableau → `onChange(filtered)`
  - [x] Clic sur une suggestion : appender la ville → `onChange([...value, city])`

- [x] Task 5 (AC: 11) — Tests unitaires
  - [x] Créer `__tests__/unit/components/shared/CityAutocomplete.test.tsx` (`/** @vitest-environment jsdom */`)
  - [x] `afterEach(() => cleanup())`
  - [x] Mode single : sélectionner une ville → `onChange` appelé avec la ville ; clic × → `onChange(null)`
  - [x] Mode multi : sélectionner 2 villes ; cliquer ↑ sur la 2e → ordre inversé ; cliquer × sur la 1re → retirée
  - [x] Mode multi : ↑ désactivé sur index=0, ↓ désactivé sur dernier

### Review Findings

_Code review du 2026-07-19 (3 couches : Blind Hunter, Edge Case Hunter, Acceptance Auditor)._

- [x] [Review][Decision] ~~AC 6 vs Task 3~~ — **Résolu (2026-07-19)** : implémentation conservée (clic badge = ouvre popover, × = efface). AC 6 reformulé pour lever l'ambiguïté.
- [x] [Review][Patch] `disabled` contournable au clavier sur le × d'effacement (mode single) — le span `role="button"` garde `tabIndex={0}` + `onKeyDown` sans garde `disabled` [src/components/shared/CityAutocomplete.tsx:179]
- [x] [Review][Patch] Élément interactif (`span role="button"`) imbriqué dans le `<button>` trigger — HTML invalide + focus/AT ambigus [src/components/shared/CityAutocomplete.tsx:172]
- [x] [Review][Patch] Erreur de la query non gérée — `error` non destructuré → un échec réseau/tRPC affiche « Aucune ville trouvée » au lieu d'un état d'erreur [src/components/shared/CityAutocomplete.tsx:72]
- [x] [Review][Patch] Pas de `maxLength={100}` sur `CommandInput` — le schéma serveur `q: z.string().max(100)` rejette silencieusement (BAD_REQUEST) au-delà [src/components/shared/CityAutocomplete.tsx:221]
- [x] [Review][Patch] `PopoverContent` (`w-(--radix-popover-trigger-width)`) se réduit à la largeur du badge en mode single sélectionné → input/suggestions tronqués [src/components/shared/CityAutocomplete.tsx:218]
- [x] [Review][Patch] Pas de `placeholderData: keepPreviousData` — flash « Recherche... » à chaque frappe ; incohérent avec le pattern du projet (candidates/search/GlobalSearchBar) [src/components/shared/CityAutocomplete.tsx:72]
- [x] [Review][Patch] « Aucune ville trouvée » affiché quand toutes les villes retournées sont déjà sélectionnées (mode multi) — l'état vide ne distingue pas « rien trouvé » de « tout déjà choisi » [src/components/shared/CityAutocomplete.tsx:249]
- [x] [Review][Patch] Trigger sélectionné (single) perd `aria-expanded`/`aria-haspopup` présents sur le trigger vide [src/components/shared/CityAutocomplete.tsx:172]
- [x] [Review][Patch] Test « exclut la ville déjà sélectionnée » n'assert pas l'absence de Paris dans les options (mock `onChange`, badge affiche « Paris ») → faux sentiment de couverture [__tests__/unit/components/shared/CityAutocomplete.test.tsx:379]
- [x] [Review][Defer] Clé React dupliquée si le parent seed `value` avec des villes en double [src/components/shared/CityAutocomplete.tsx:125] — deferred, dépend d'un mauvais usage côté parent
- [x] [Review][Defer] Cosmétique debounce : flicker « Saisir au moins 3 caractères » + suggestions stale ~300ms pendant la fenêtre de debounce [src/components/shared/CityAutocomplete.tsx:70] — deferred, inhérent au debounce

## Dev Notes

### Previous Story Insights

- Story 5.2 terminée : `trpc.cities.autocomplete` disponible
- Le projet utilise shadcn/ui `Popover` + `Command` pour le pattern Combobox (voir `src/components/ui/`)

### Component Specifications

- **Debounce :** si `useDebounce` n'existe pas encore dans le projet, utiliser le pattern `useState` + `useEffect` avec `setTimeout` plutôt que d'ajouter une dépendance. Si `use-debounce` est déjà dans `package.json`, l'utiliser.
- **Popover width :** `w-(--radix-popover-trigger-width)` sur `<PopoverContent>` pour aligner la largeur sur le trigger (convention projet CLAUDE.md)
- **Chips mode multi :** utiliser `<Badge variant="secondary">` ou un composant `<div>` stylé en flexbox avec `gap-1` — rester cohérent avec les chips de tags existants dans le projet
- **Boutons ↑↓ :** utiliser `<Button variant="ghost" size="icon">` avec `ChevronUp` / `ChevronDown` de `lucide-react`

### File Locations

| Fichier | Action |
|---------|--------|
| `src/components/shared/CityAutocomplete.tsx` | Créé |
| `tests/unit/components/shared/CityAutocomplete.test.tsx` | Créé |

[Source: docs/architecture/source-tree.md]

### Technical Constraints

- Pas de `console.log` — uniquement `console.error` si besoin
- `isLoading` (pas `isFetching`) pour l'état skeleton/loading (convention CLAUDE.md)
- `"use client"` requis sur ce composant (interactivité)
- Le composant ne doit pas gérer le `order` en interne : il expose `CityOption[]` dans l'ordre courant du tableau — c'est le parent qui décide de l'`order` à persister

### Testing

- Tests RTL avec `/** @vitest-environment jsdom */` et `afterEach(() => cleanup())`
- Mock `trpc.cities.autocomplete` via `vi.mock`

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-14 | 0.1 | Story draft créée | John (PM) |
| 2026-07-19 | 1.0 | Composant `CityAutocomplete` implémenté (single + multi), tests unitaires, story → review | Amelia (Dev) |
| 2026-07-19 | 1.1 | Code review 3 couches : 9 patchs appliqués (a11y badge/disabled, gestion erreur, maxLength, largeur popover, keepPreviousData, message « tout sélectionné », ARIA, test AC 9), 2 items différés, AC 6 clarifié → done | Code Review |

---

## Dev Agent Record

### Agent Model Used

claude-opus-4-8 (1M context)

### Debug Log References

- `pnpm vitest run __tests__/unit/components/shared/CityAutocomplete.test.tsx` → 8/8 ✅
- `pnpm exec tsc --noEmit` → 0 erreur ✅
- `pnpm exec eslint` (fichiers concernés) → 0 erreur/warning ✅
- `pnpm test:unit` (suite complète) → 34 fichiers / 314 tests ✅ (exit 0)

### Completion Notes List

- **Écart doc → code (procédure tRPC)** : la story référence `trpc.cities.autocomplete`, mais le router est monté sous `city` dans `_app.ts`. Utilisé le nom réel : `api.city.autocomplete` (import `api` depuis `@/lib/trpc/client`).
- **Écart doc → code (chemin des tests)** : le tableau File Locations indiquait `tests/unit/...`, mais l'arbo réelle (commit 2a64113) est `__tests__/` à la racine. Test créé sous `__tests__/unit/components/shared/CityAutocomplete.test.tsx`.
- **`useDebounce`** déjà présent (`@/hooks/useDebounce`) — réutilisé, aucune dépendance ajoutée.
- **Rendu des suggestions découplé du debounce** : la liste s'affiche dès que `city.autocomplete` renvoie des résultats (le gating < 3 caractères reste assuré côté requête via `enabled`). Cela rend les tests déterministes sans manipuler les timers.
- **Fuite async jsdom corrigée** : ouvrir un Popover Radix planifie du travail via le scheduler React (MessageChannel) qui se déclenchait après le teardown jsdom (`window is not defined`, faisait échouer `pnpm test`). Résolu par un flush `act(async () => setTimeout 0)` en `afterEach`, avant le teardown. Polyfills jsdom nécessaires ajoutés dans le test (`ResizeObserver`, `scrollIntoView`, `*PointerCapture`).
- **`order` non géré en interne** (contrainte Dev Notes) : le composant expose `CityOption[]` dans l'ordre courant ; c'est au parent de persister l'`order`.

### File List

| Fichier | Action |
|---------|--------|
| `src/components/shared/CityAutocomplete.tsx` | Créé |
| `__tests__/unit/components/shared/CityAutocomplete.test.tsx` | Créé |
| `docs/implementation-artifacts/sprint-status.yaml` | Modifié (entrée 5-3 → review) |

---

## QA Results

*(À remplir par l'agent QA)*
