# Story 4.3: Job Offer List Filters

## Status

DONE

## Story

**As a** recruiter,
**I want** to filter job offers by status, salary, location, and tags,
**so that** I can find specific offers quickly.

## Acceptance Criteria

1. Filter panel on offers list page
2. Filter by status: checkboxes (À faire, En cours, Terminé)
3. Filter by tags: multi-select
4. Filter by salary range: min/max inputs
5. Filter by location/city: text input
6. Filter by client company: dropdown
7. Multiple filters combine with AND logic
8. Clear filters button
9. Active filters shown as chips
10. Filtered count: "X offres trouvées"

**Réf.** Wireframes §4 Liste Offres (filtres Statut, Tags, Salaire, Client, Ville)

## Tasks / Subtasks

- [x] Task 0 – Standards & architecture (pré-requis)
  - [x] Lire `docs/architecture/coding-standards.md`, `docs/frontend-architecture.md` et `docs/architecture.md` avant de commencer l'implémentation
  - [x] S'assurer que la structure des routers tRPC, les patterns frontend, l'approche de tests et l'a11y suivent ces guides

- [x] Task 1 (AC: 1, 2, 3, 4, 5, 6, 7, 10): API – Étendre `offer.list` pour les filtres
  - [x] Étendre `offerListInputSchema` dans `src/lib/validations/offer.ts` avec des champs optionnels pour les filtres
    - [x] `statuses?: JobOfferStatus[]` (multi-sélection correspondant à `JobOfferStatus` Prisma)
    - [x] `tagIds?: string[]` (UUID des tags)
    - [x] `salaryMin?: number`, `salaryMax?: number` (nombres entiers non négatifs)
    - [x] `location?: string` (trim → undefined si vide)
    - [x] `clientCompanyId?: string` (UUID, optionnel)
  - [x] Dans `src/server/trpc/routers/offer.ts`, construire un `where` qui conserve toujours `companyId: ctx.companyId`
  - [x] Filtre statut (AC 2) : si `statuses` non vide, appliquer `status: { in: statuses }`
  - [x] Filtre tags (AC 3 + AC 7 AND logic) : si `tagIds` non vide, appliquer une contrainte AND
    - [x] Pour chaque `tagId` sélectionné, exiger qu'au moins un lien `OfferTag` correspond via une condition `AND` (1 condition par tag)
  - [x] Filtre salaire (AC 4) : appliquer un filtrage cohérent avec des bornes min/max
    - [x] Si `salaryMin` fourni : garder les offres dont `salaryMax >= salaryMin`
    - [x] Si `salaryMax` fourni : garder les offres dont `salaryMin <= salaryMax`
    - [x] Les offres avec valeurs `null` sur les champs salaires ne doivent pas matcher les bornes fournies
  - [x] Filtre location/city (AC 5) : si `location` fourni, appliquer une recherche case-insensitive sur `JobOffer.location`
  - [x] Filtre client company (AC 6) : si `clientCompanyId` fourni, appliquer `clientCompanyId: <id>`
  - [x] Mettre à jour aussi la requête `count` pour retourner un `totalCount` filtré (AC 10)
  - [x] Conserver la forme de la réponse existante (`items`, `totalCount`, `page`, `pageSize`) pour éviter les régressions
  - [x] S'assurer que toutes les requêtes utilisent bien `protectedProcedure` et `ctx.companyId` pour l'isolation multi-tenancy [Source: docs/architecture.md §4, docs/architecture/coding-standards.md §3]

- [x] Task 2 (AC: 1, 2, 3, 4, 5, 6, 8): UI – Composant panneau de filtres offres
  - [x] Créer `src/components/offers/OfferListFilters.tsx`
  - [x] Props : `filters`, `onFiltersChange`, `onClear`
  - [x] Statuts (AC 2) : implémenter une UI type checkboxes pour `TODO`, `IN_PROGRESS`, `DONE`
  - [x] Tags (AC 3) : Popover multi-sélection avec check-icon (pattern proche de `CandidateListFilters`)
    - [x] Source : `api.tag.list.useQuery()`
  - [x] Salaire (AC 4) : 2 inputs numériques `salaryMin`/`salaryMax` (min 0)
  - [x] Location/city (AC 5) : input texte
  - [x] Client company (AC 6) : dropdown via `api.clientCompany.list.useQuery()` + composant `Select`
    - [x] Inclure une option par défaut "Tous" (pas de filtre)
  - [x] Bouton "Effacer filtres" (AC 8) :
    - [x] visible seulement si au moins un filtre est actif

- [x] Task 3 (AC: 9, 10): UI – Chips de filtres actifs + comptage
  - [x] Créer `src/components/offers/ActiveOfferFilterChips.tsx`
  - [x] Afficher des chips pour chaque filtre actif :
    - [x] Statuts (labels "À faire", "En cours", "Terminé")
    - [x] Tags sélectionnés
    - [x] Salaire (min/max)
    - [x] Location/city
    - [x] Client company
  - [x] Chaque chip doit permettre de retirer uniquement le filtre correspondant
  - [x] Afficher le texte `X offres trouvées` (AC 10) basé sur `totalCount` filtré
  - [x] Utiliser un `aria-live` pour annoncer le comptage pendant le chargement

- [x] Task 4 (AC: 1, 7, 8, 9, 10): Intégration dans la page `/offers`
  - [x] Refactorer `src/app/(dashboard)/offers/page.tsx` pour intégrer :
    - [x] `OfferListFilters` en haut de la liste
    - [x] `ActiveOfferFilterChips` quand des filtres sont actifs
    - [x] `api.offer.list.useQuery` avec les nouveaux paramètres de filtres (en plus du tri existant)
  - [x] Préserver le comportement existant :
    - [x] skeleton pendant le chargement (via `isLoading`)
    - [x] états vides et erreur
  - [x] S'assurer que les filtres combinés suivent bien l'AND logic (AC 7)

- [x] Task 5 – Testing (AC: 1–10)
  - [x] Tests d'intégration (Vitest) pour `offer.list` :
    - [x] statut multi-sélection
    - [x] tags multi-sélection avec AND logic
    - [x] salaryMin / salaryMax (cas partiel + cas combiné)
    - [x] location/city
    - [x] client company dropdown
    - [x] combinaison de plusieurs filtres avec AND
    - [x] multi-tenancy : aucune fuite de données cross-cabinet
  - [x] Tests unitaires (Vitest) pour les schémas Zod de validation des inputs de filtres
  - [x] Tests de composants (au minimum) :
    - [x] rendu du panneau de filtres
    - [x] interaction tags (sélection) + retrait via chips
    - [x] affichage du libellé "X offres trouvées"
  - [x] Suivre la stratégie et les emplacements de tests Vitest décrits dans l'architecture [Source: docs/architecture.md §14]

- [x] Task 6 (QA): Rédiger les cas de test dans un fichier dédié (ex. `docs/qa/` ou `docs/stories/cases/4.3-test-cases.md`) pour faciliter la review de la story

- [x] Task 7 (review): Lancer une review de code en fin de développement avant clôture de la story

## Dev Notes

### Previous Story Insights

- Story `4.1` : barre de recherche globale (pattern modal/UX) et structure globale côté dashboard
- Story `4.2` DONE : filtres liste candidats (pattern UI Popover multi-sélection + chips actifs) et conventions de synchronisation de l'état dans l'UI
- Story `3.3` DONE : page `/offers` existante (liste d'offres + tri, skeleton, empty state) et router `offer.list` (pagination + tri)

### Data Models

- `JobOfferStatus` : `TODO`, `IN_PROGRESS`, `DONE` (Prisma)
- `JobOffer` : `title`, `description?`, `location?`, `salaryMin?`, `salaryMax?`, `status`, `clientCompanyId?`, `companyId`, `createdAt`, `updatedAt`
- `OfferTag` : relation N–N entre `JobOffer` et `Tag` via `offerId` + `tagId`
- `ClientCompany` : `id`, `name`, `siren?`, `companyId`
- `Tag` : `id`, `name`, `color`, `companyId`

### API Specifications

- `offer.list` (protectedProcedure) :
  - aujourd'hui : pagination + tri (`sortBy` whitelist et `sortOrder`)
  - extension attendue : ajout de filtres optionnels (statuts, tags, salaire, location, client company)
  - toutes les requêtes doivent rester scopées par `ctx.companyId`
- `totalCount` :
  - doit refléter le nombre d'offres après application des filtres (AC 10)
  
### Component Specifications

- Panneau de filtres :
  - doit être accessible (labels, navigation clavier) conformément aux patterns UI du projet [Source: docs/frontend-architecture.md §9]
  - statuts : présentation "checkboxes" (multi-sélection)
  - tags : Popover multi-sélection avec état "sélectionné"
  - salaire : inputs numériques `min/max`
  - location : texte libre
  - client company : dropdown via liste de `ClientCompany`
- Chips de filtres actifs :
  - pattern proche de `ActiveFilterChips` (chips + bouton X + comptage) [Source: docs/frontend-architecture.md §4.4]

### File Locations

- Modifications :
  - `src/lib/validations/offer.ts` (input schema filtres)
  - `src/server/trpc/routers/offer.ts` (where + count filtré)
  - `src/app/(dashboard)/offers/page.tsx` (intégration UI + appels API)
- Créations :
  - `src/components/offers/OfferListFilters.tsx`
  - `src/components/offers/ActiveOfferFilterChips.tsx`

### Technical Constraints

- Multi-tenancy : préserver `companyId: ctx.companyId` dans toutes les requêtes côté API
- AND logic (AC 7) :
  - pour `tagIds`, appliquer une contrainte AND (une condition par tag sélectionné)
- Skeleton/Loading :
  - utiliser `isLoading` pour le skeleton (et éviter des flashes avec `isFetching`), conformément aux conventions frontend du projet
  - [Source: docs/frontend-architecture.md §5, docs/architecture.md §10.2]

### Testing

- Architecture de tests : privilégier
  - tests d'intégration côté API (Vitest) avec contexte `companyId` pour vérifier l'isolation multi-tenant
  - tests unitaires pour la validation Zod des inputs
- Vérifier particulièrement :
  - combinaisons multi-filtres (AND logic)
  - cas limites (ex. 0 offre, filtres partiels, champs salaires/location absents)
  - [Source: docs/architecture.md §14]

### Project Structure Notes

- Pas de conflit attendu avec la page `/offers` existante : la story étend les mêmes endpoints et composants (router `offer`, cards `JobOfferCard`)
- Les filtres offres doivent être factorisés dans des composants sous `src/components/offers/` pour éviter la duplication avec `CandidateListFilters`

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-25 | 0.1 | Story draft created (4.3 Job Offer List Filters) | Bob (SM) |
| 2026-03-25 | 1.0 | Implementation complete — all tasks done | James (Dev Agent) |

---

## Dev Agent Record

### Agent Model Used

Claude claude-4.6-opus (Cursor Agent)

### Debug Log References

*(Aucune)*

### Completion Notes List

- Schema Zod etendu avec 6 champs de filtres optionnels (statuses, tagIds, salaryMin, salaryMax, location, clientCompanyId)
- jobOfferStatusSchema deplace avant offerListInputSchema pour resoudre la dependance circulaire
- Router offer.list : construction dynamique du where Prisma avec AND logic pour les tags (1 condition some par tag)
- Filtre salaire : logique croisee (salaryMax >= input.salaryMin et salaryMin <= input.salaryMax)
- Location : recherche contains case-insensitive
- totalCount utilise maintenant le meme where filtre
- UI : pattern coherent avec Story 4.2 (CandidateListFilters) — Popover multi-selection, debounce location, Select pour client
- API client : api.clientCompany.list (nom du router dans _app.ts)
- 225 tests passent (0 echec), dont 27 tests Zod, 19 tests composants, 12 tests integration filtres
- TypeScript strict : 0 erreur. ESLint : 0 erreur.

### File List

| Path | Action |
|------|--------|
| src/lib/validations/offer.ts | Modified |
| src/server/trpc/routers/offer.ts | Modified |
| src/app/(dashboard)/offers/page.tsx | Modified |
| src/components/offers/OfferListFilters.tsx | Created |
| src/components/offers/ActiveOfferFilterChips.tsx | Created |
| tests/unit/validations/offer.test.ts | Modified |
| tests/unit/components/offers/OfferListFilters.test.tsx | Created |
| tests/unit/components/offers/ActiveOfferFilterChips.test.tsx | Created |
| tests/integration/offer/offer.test.ts | Modified |
| docs/stories/cases/4.3-test-cases.md | Created |

---

## QA Results

*(Results from QA Agent review of the completed story implementation.)*
