# Story 5.6: Formulaire entreprise cliente — Villes (multi, sans ordre)

## Status

review

## Story

**As a** recruiter,
**I want** to associate multiple cities to a client company,
**so that** I know which locations a client operates in.

## Acceptance Criteria

1. Formulaire création entreprise cliente : `CityAutocomplete` en `mode="multi"` ajouté — les boutons ↑↓ ne s'affichent pas (pas d'ordre pour `ClientCompanyCity`)
2. Formulaire édition entreprise cliente : même ajout ; villes actuelles pré-chargées
3. Procédure `clientCompany.create` : accepte `cityIds: string[]` (peut être vide)
4. Procédure `clientCompany.update` : remplace toutes les `ClientCompanyCity` (`deleteMany` puis `createMany`)
5. Schéma Zod mis à jour : `cityIds` ajouté (`z.array(z.uuid()).default([])`)
6. Fiche entreprise cliente (`/clients/[id]`) : villes affichées comme badges (sans ordre imposé)
7. Include `cities { city: true }` dans `clientCompany.getById`
8. Tests : create avec villes ; update remplace les villes ; create sans villes fonctionne

## Tasks / Subtasks

- [x] Task 0 — Pré-requis
  - [x] Vérifier que la story 5.3 est DONE (composant `CityAutocomplete` disponible)
  - [x] Lire `src/server/trpc/routers/client.ts` en intégralité (le fichier/router s'appelle `client`/`clientRouter`, exposé sous `clientCompany` dans l'app — pas `clientCompany.ts`, cf. story 3.1)
  - [x] Lire `src/lib/validations/client.ts` en intégralité
  - [x] Lire le formulaire de création et la page fiche client — **aucun formulaire d'édition n'existait** (pas de page `/clients/[id]/edit`, pas de procédure `update`, pas de `updateClientCompanySchema`) ; clarifié avec Dorian, cf. Completion Notes

- [x] Task 1 (AC: 5) — Mettre à jour `src/lib/validations/client.ts`
  - [x] Ajouter `cityIds: z.array(z.uuid()).default([])` dans `createClientCompanySchema` et `updateClientCompanySchema` (nouveau, n'existait pas)

- [x] Task 2 (AC: 3, 7) — Mettre à jour `clientCompany.create`
  - [x] Accepter `cityIds` dans l'input
  - [x] Après la création, insérer les villes (dans une transaction `$transaction`, pattern aligné sur `candidate.create`)
  - [x] Dans `clientCompany.getById` : ajouté `include: { cities: { include: { city: true } } }`

- [x] Task 3 (AC: 4) — Créer `clientCompany.update` (nouvelle procédure, n'existait pas)
  - [x] Vérifie l'ownership (companyId), met à jour name/siren, puis remplace les `ClientCompanyCity` (`deleteMany` puis `createMany`) dans une transaction

- [x] Task 4 (AC: 1) — Mettre à jour le formulaire de création entreprise cliente
  - [x] `CityAutocomplete` en `mode="multi"` `ordered={false}` (nouvelle prop sur le composant, remplace l'option `showOrder`/ordre envisagée dans la note technique — cf. Dev Notes)
  - [x] State local : `selectedCities: CityOption[]`
  - [x] À la soumission : `cityIds: selectedCities.map((city) => city.id)`
  - [x] Formulaire extrait en composant partagé `ClientCompanyForm` (mode create/edit) pour rester DRY, à la demande de Dorian

- [x] Task 5 (AC: 2) — Formulaire d'édition entreprise cliente
  - [x] Créé `src/app/(dashboard)/clients/[id]/edit/page.tsx` (n'existait pas) réutilisant `ClientCompanyForm` en mode "edit"
  - [x] Pré-charge les villes depuis `clientCompany.getById` → `cities: [{ city: {...} }]`, mappées vers `CityOption[]`
  - [x] Même logique de soumission que Task 4 (composant partagé)

- [x] Task 6 (AC: 6) — Mettre à jour la fiche entreprise cliente
  - [x] Section "Villes" avec badges (`Badge` variant secondary), vide si aucune ville associée
  - [x] Bouton "Modifier" ajouté dans l'en-tête, vers `/clients/[id]/edit`

- [x] Task 7 (AC: 8) — Tests
  - [x] Test `clientCompany.create` avec `cityIds: [id1, id2]` → 2 `ClientCompanyCity` en base, `getById` les retourne
  - [x] Test `clientCompany.update` remplace les villes (deleteMany puis createMany)
  - [x] Test `clientCompany.create` sans `cityIds` → pas d'erreur, relation vide
  - [x] Test `clientCompany.update` NOT_FOUND autre cabinet
  - [x] Tests unitaires Zod (`createClientCompanySchema`/`updateClientCompanySchema`)
  - [x] Tests `CityAutocomplete` prop `ordered` (masque/affiche les boutons ↑↓)

## Dev Notes

### Previous Story Insights

- Story 5.1 : modèle `ClientCompanyCity` créé en base (`@@id([clientCompanyId, cityId])` — pas d'`id` auto, pas d'`order`)
- Story 5.3 : `CityAutocomplete` disponible

### Data Models

```
ClientCompanyCity {
  clientCompanyId  String   (PK, FK ClientCompany, onDelete Cascade)
  cityId           String   (PK, FK City, onDelete Restrict)
  @@id([clientCompanyId, cityId])
}
```

Pas d'`order` : `ClientCompanyCity` est une liaison N-N pure. Les villes s'affichent sans ordre particulier.

### API Specifications

**Gestion des boutons ↑↓ dans `CityAutocomplete` :**  
Le composant `CityAutocomplete` affiche les boutons ↑↓ en mode multi. Pour `ClientCompany` où l'ordre ne compte pas, deux options :
- **Option A (préférable)** : ajouter une prop `ordered?: boolean` (défaut `true`) sur `CityAutocomplete` — si `false`, les boutons ↑↓ ne sont pas rendus
- **Option B (acceptable)** : ignorer l'ordre retourné par `onChange`, utiliser simplement les IDs

Préférer l'option A pour l'UX (évite une confusion pour le recruteur).

### File Locations

| Fichier | Action |
|---------|--------|
| `src/lib/validations/clientCompany.ts` | Modifié (ajout cityIds) |
| `src/server/trpc/routers/clientCompany.ts` | Modifié (create, update, getById) |
| Formulaire création entreprise cliente | Modifié |
| Formulaire édition entreprise cliente | Modifié |
| Page fiche entreprise cliente | Modifiée (affichage) |
| `src/components/shared/CityAutocomplete.tsx` | Modifié si prop `ordered` ajoutée |
| `tests/integration/clientCompany/clientCompany-cities.test.ts` | Créé |

### Technical Constraints

- Invalider `utils.clientCompany.getById.invalidate()` après les mutations
- `ClientCompanyCity` n'a pas d'`id` propre — les opérations Prisma utilisent la PK composite `[clientCompanyId, cityId]`

### Testing

- Tests d'intégration dans `tests/integration/clientCompany/` avec Prisma réel

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-14 | 0.1 | Story draft créée | John (PM) |
| 2026-08-16 | 1.0 | Implémentation complète : `cityIds` sur create/update, procédure `clientCompany.update` (nouvelle), prop `ordered` sur `CityAutocomplete`, formulaire partagé `ClientCompanyForm` (create/edit, DRY), page d'édition `/clients/[id]/edit` (nouvelle), badges villes + bouton Modifier sur la fiche client, tests unitaires et d'intégration | Amelia (Dev) |

---

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (Amelia, bmad-dev-story)

### Debug Log References

N/A

### Completion Notes List

- **Écart avec la story constaté au Task 0** : aucun formulaire d'édition ni procédure `update` n'existaient pour `ClientCompany` (seuls `create`, `list`, `getById` existaient — story 3.1). Les tâches 2/5 de la story supposaient à tort qu'il suffisait d'étendre un formulaire d'édition déjà présent. Clarifié avec Dorian via question : option retenue = réutiliser le formulaire de création (composant partagé `ClientCompanyForm`, mode `create`/`edit`) plutôt qu'une modale dédiée sur la fiche client, pour rester DRY — miroir du pattern déjà utilisé par `JobOfferForm`/`/offers/[id]/edit`.
- `client.ts` (router) / `client.ts` (validations) sont les vrais noms de fichiers (le router `clientRouter` est exposé sous `clientCompany` dans `_app.ts`, cf. story 3.1) — la story mentionnait `clientCompany.ts`.
- `CityAutocomplete` : nouvelle prop `ordered?: boolean` (défaut `true`) — masque les boutons ↑↓ en mode multi quand `false`. Utilisée avec `ordered={false}` pour les villes de `ClientCompany` (relation N-N pure, sans `order`, contrairement à `CandidateCity`).
- `clientCompany.create`/`clientCompany.update` : insertion/remplacement des `ClientCompanyCity` dans un `$transaction` (pattern aligné sur `candidate.create`/`candidate.update`), pour éviter un client orphelin si le `createMany` échoue.
- Fiche client (`/clients/[id]`) : ajout d'un bouton "Modifier" dans l'en-tête (vers `/clients/[id]/edit`) et d'une section "Villes" (badges `variant="secondary"`, vide si aucune ville).
- Travaillé directement sur la branche `5.6` dans le checkout principal (pas de worktree isolé) à la demande explicite de Dorian — `.claude/settings.json` mis à jour (`worktree.bgIsolation: "none"`).
- Validations : `pnpm exec tsc --noEmit` (0 erreur), `pnpm lint` (0 erreur), `pnpm test:unit` (344 passed / 3 todo — cf. note flake ci-dessous), `pnpm test:db` (212 passed / 14 skipped / 5 todo, aucune régression).
- Note test infra (hors scope) : `pnpm test:unit` affiche parfois des "Unhandled Errors" (`ReferenceError: window is not defined`) sur des fichiers *non liés* à cette story (ex. `OfferDetailView.test.tsx`, `CandidateCandidaturesSection.test.tsx`), de façon non déterministe (absent sur 1 run sur 3). Confirmé pré-existant et sans lien avec cette story : reproductible aussi sur `main` avant ces changements, race connue entre le scheduler React (setImmediate/MessageChannel) et le teardown jsdom sous workers parallèles Vitest (déjà documentée dans un commentaire de `CityAutocomplete.test.tsx`). Aucune assertion ne casse (344/344 tests passent à chaque run) ; non traité ici, hors scope de la story.
- Note formatting (hors scope) : `pnpm format:check` signale ~284 fichiers non conformes à travers tout le repo (y compris des fichiers jamais touchés comme `tsconfig.json`) — dérive Prettier pré-existante (probablement un écart de version), non liée à cette story. Les fichiers modifiés suivent le même style que le code existant autour d'eux ; non reformaté pour éviter un diff massif hors-sujet.
- Vérification UI interactive non effectuée : aucun outil de navigateur/capture d'écran n'était disponible dans cette session. Couverture par tests automatisés (unitaires + intégration DB) uniquement.

### File List

| Fichier | Action |
|---------|--------|
| `src/lib/validations/client.ts` | Modifié (`cityIds` sur `createClientCompanySchema`, nouveau `updateClientCompanySchema`) |
| `src/server/trpc/routers/client.ts` | Modifié (`create` insère les villes, `getById` inclut `cities`, nouvelle procédure `update`) |
| `src/components/shared/CityAutocomplete.tsx` | Modifié (nouvelle prop `ordered`) |
| `src/components/clients/ClientCompanyForm.tsx` | Créé (formulaire partagé create/edit) |
| `src/app/(dashboard)/clients/new/page.tsx` | Modifié (utilise `ClientCompanyForm`) |
| `src/app/(dashboard)/clients/[id]/edit/page.tsx` | Créé |
| `src/app/(dashboard)/clients/[id]/page.tsx` | Modifié (bouton Modifier, section Villes) |
| `__tests__/unit/lib/validations/client.test.ts` | Modifié (tests `createClientCompanySchema`/`updateClientCompanySchema`) |
| `__tests__/unit/components/shared/CityAutocomplete.test.tsx` | Modifié (tests prop `ordered`) |
| `__tests__/db/server/trpc/routers/client.test.ts` | Modifié (tests `cities (ClientCompanyCity)` : create/update/getById) |
| `.claude/settings.json` | Créé (`worktree.bgIsolation: "none"`, config harness — hors périmètre applicatif) |

---

## QA Results

*(À remplir par l'agent QA)*
