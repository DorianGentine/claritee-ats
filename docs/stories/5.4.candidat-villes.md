# Story 5.4: Formulaire candidat — Villes (multi, ordonné)

## Status

TODO

## Story

**As a** recruiter,
**I want** to associate multiple ordered cities to a candidate,
**so that** I can reflect geographic preferences on the profile.

## Acceptance Criteria

1. Formulaire création candidat (`/candidates/new`) : champ `city` (texte) remplacé par `CityAutocomplete` en `mode="multi"`
2. Formulaire édition candidat (`/candidates/[id]/edit`) : même remplacement ; villes actuelles pré-chargées dans le bon ordre
3. Procédure `candidate.create` : accepte `cities: { cityId: string; order: number }[]` (peut être vide)
4. Procédure `candidate.update` : remplace toutes les `CandidateCity` existantes (`deleteMany` puis `createMany`)
5. Schéma Zod mis à jour : `city` retiré, `cities` ajouté (`z.array(z.object({ cityId: z.uuid(), order: z.number().int().min(0) })).default([])`)
6. Fiche candidat (`/candidates/[id]`) : villes affichées dans l'ordre (ex. "Paris · Lyon")
7. Liste candidats (`/candidates`) : affiche la ville `order=0` du candidat, ou vide si aucune
8. Include `cities { city: true, order: true }` trié par `order asc` dans `candidate.getById` et `candidate.list`
9. Tests : create avec 2 villes + vérification ordre en base ; update remplace correctement ; create sans villes fonctionne

## Tasks / Subtasks

- [ ] Task 0 — Pré-requis
  - [ ] Vérifier que la story 5.3 est DONE (composant `CityAutocomplete` disponible)
  - [ ] Lire `src/server/trpc/routers/candidate.ts` en intégralité
  - [ ] Lire `src/lib/validations/candidate.ts` en intégralité
  - [ ] Lire `src/app/(dashboard)/candidates/new/page.tsx` et le formulaire de création
  - [ ] Lire `src/app/(dashboard)/candidates/[id]/edit/page.tsx` (ou équivalent) et `src/app/(dashboard)/candidates/[id]/page.tsx`

- [ ] Task 1 (AC: 5) — Mettre à jour `src/lib/validations/candidate.ts`
  - [ ] Retirer le champ `city: z.string().optional()...`
  - [ ] Ajouter :
    ```ts
    cities: z.array(
      z.object({ cityId: z.uuid(), order: z.number().int().min(0) })
    ).default([]),
    ```
  - [ ] Appliquer à tous les schemas concernés (`createCandidateSchema`, `updateCandidateSchema`)

- [ ] Task 2 (AC: 3, 8) — Mettre à jour `candidate.create` dans le router
  - [ ] Accepter `cities` dans l'input
  - [ ] Après `prisma.candidate.create(...)`, insérer les villes :
    ```ts
    if (input.cities.length > 0) {
      await ctx.db.candidateCity.createMany({
        data: input.cities.map((cityInput) => ({
          candidateId: created.id,
          cityId: cityInput.cityId,
          order: cityInput.order,
        })),
      })
    }
    ```
  - [ ] Dans `candidate.list` : ajouter `include: { cities: { include: { city: true }, orderBy: { order: 'asc' }, take: 1 } }` pour récupérer la ville principale (order=0)
  - [ ] Dans `candidate.getById` : ajouter `include: { cities: { include: { city: true }, orderBy: { order: 'asc' } } }`

- [ ] Task 3 (AC: 4) — Mettre à jour `candidate.update` dans le router
  - [ ] Dans la mutation `update`, après la mise à jour des champs scalaires :
    ```ts
    await ctx.db.candidateCity.deleteMany({ where: { candidateId: input.id } })
    if (input.cities.length > 0) {
      await ctx.db.candidateCity.createMany({
        data: input.cities.map((cityInput) => ({
          candidateId: input.id,
          cityId: cityInput.cityId,
          order: cityInput.order,
        })),
      })
    }
    ```
  - [ ] Retirer toute référence à `city` (string) dans `candidate.update`

- [ ] Task 4 (AC: 1) — Mettre à jour le formulaire de création (`/candidates/new`)
  - [ ] Localiser le champ `city` dans le formulaire React Hook Form
  - [ ] Remplacer par `<CityAutocomplete mode="multi" value={...} onChange={...} />`
  - [ ] Le state local du composant gérant le formulaire : `cities: CityOption[]`
  - [ ] À la soumission, mapper vers `{ cityId, order }` :
    ```ts
    cities: formCities.map((city, index) => ({ cityId: city.id, order: index }))
    ```

- [ ] Task 5 (AC: 2) — Mettre à jour le formulaire d'édition (`/candidates/[id]/edit`)
  - [ ] Pré-charger les villes depuis `candidate.getById` dans l'ordre
  - [ ] Mapper les `CandidateCity` vers `CityOption[]` pour initialiser `CityAutocomplete`
  - [ ] Même logique de soumission que Task 4

- [ ] Task 6 (AC: 6, 7) — Mettre à jour l'affichage
  - [ ] Fiche candidat : remplacer l'affichage de `candidate.city` par les villes structurées (jointure `·` ou liste)
  - [ ] Liste candidats : afficher `candidate.cities[0]?.city.name` (ou vide)

- [ ] Task 7 (AC: 9) — Tests
  - [ ] Test intégration `candidate.create` avec `cities: [{cityId, order:0}, {cityId2, order:1}]` → vérifier 2 `CandidateCity` en base dans le bon ordre
  - [ ] Test `candidate.update` remplace les villes (old → new)
  - [ ] Test `candidate.create` sans `cities` → fonctionne, `cities` vide en base
  - [ ] Réactiver les tests `.todo` dans `tests/unit/validations/candidate.test.ts` (mis en attente en story 5.1) :
    - `candidateListInputSchema` : "accepts city string and trims whitespace" et "converts empty city to undefined" → adapter pour `cities: z.array(...)`
    - `createCandidateSchema` : "trims optional fields..." et "converts whitespace-only optional fields to undefined" → adapter pour le champ `cities`
    - `updateCandidateSchema` : "accepts all base fields" → adapter pour inclure `cities`

## Dev Notes

### Previous Story Insights

- Story 5.1 : `Candidate.city` n'existe plus en base — le champ a été supprimé dans la migration. **Ne pas référencer `city` dans les requêtes Prisma.**
- Story 5.3 : `CityAutocomplete` disponible dans `src/components/shared/CityAutocomplete.tsx`
- Le router `candidate.ts` contient une procédure `listCities` qui renvoyait des villes distinctes en string — elle sera supprimée en story 5.7. Ne pas la modifier ici.

### Data Models

```
CandidateCity {
  id          String   (PK uuid)
  candidateId String   (FK Candidate, onDelete Cascade)
  cityId      String   (FK City, onDelete Restrict)
  order       Int      (0-based)
  @@unique([candidateId, cityId])
}
```

**Pattern delete+createMany pour `update` :** Prisma ne gère pas nativement la mise à jour d'une liste ordonnée N-N. Le pattern delete+createMany est la bonne approche ici. Pas besoin de `$transaction` (pas de `groupBy` concerné).

### API Specifications

**`candidate.create` input (extrait) :**
```ts
cities: z.array(z.object({ cityId: z.uuid(), order: z.number().int().min(0) })).default([])
```

**`candidate.list` output (extrait) :**
```ts
cities: [{ city: { id, name, region, country }, order: 0 }]  // take: 1, orderBy order asc
```

**`candidate.getById` output (extrait) :**
```ts
cities: [
  { city: { id, name, region, country }, order: 0 },
  { city: { id, name, region, country }, order: 1 },
]
```

### File Locations

| Fichier | Action |
|---------|--------|
| `src/lib/validations/candidate.ts` | Modifié (city → cities) |
| `src/server/trpc/routers/candidate.ts` | Modifié (create, update, list, getById) |
| `src/app/(dashboard)/candidates/new/page.tsx` (ou composant form) | Modifié |
| `src/app/(dashboard)/candidates/[id]/edit/page.tsx` (ou composant form) | Modifié |
| `src/app/(dashboard)/candidates/[id]/page.tsx` | Modifié (affichage villes) |
| `src/app/(dashboard)/candidates/page.tsx` | Modifié (affichage ville principale en liste) |
| `tests/integration/candidate/candidate-cities.test.ts` | Créé |

### Technical Constraints

- L'`order` dans `CityAutocomplete` est géré par la **position dans le tableau** `CityOption[]`. À la soumission : `formCities.map((city, index) => ({ cityId: city.id, order: index }))`.
- Ne pas utiliser `$transaction` avec `groupBy` (interdit selon CLAUDE.md) — le delete+createMany seul ne nécessite pas de `$transaction` dans ce cas.
- Invalider `utils.candidate.getById.invalidate()` et `utils.candidate.list.invalidate()` après `update`.

### Testing

- Tests d'intégration dans `tests/integration/candidate/` avec Prisma réel (pas de mock DB)
- Tester `candidate.update` avec des villes différentes pour vérifier le remplacement complet

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-14 | 0.1 | Story draft créée | John (PM) |

---

## Dev Agent Record

*(À remplir par l'agent de développement)*

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

---

## QA Results

*(À remplir par l'agent QA)*
