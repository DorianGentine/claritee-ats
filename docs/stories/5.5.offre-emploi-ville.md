# Story 5.5: Formulaire offre d'emploi — Ville unique

## Status

TODO

## Story

**As a** recruiter,
**I want** to associate a single structured city to a job offer,
**so that** offers have consistent, filterable locations.

## Acceptance Criteria

1. Formulaire création offre : champ `location` (texte) remplacé par `CityAutocomplete` en `mode="single"`
2. Formulaire édition offre : même remplacement ; ville actuelle pré-chargée si définie
3. Procédure `jobOffer.create` : `location` retiré, `cityId: string | null` ajouté
4. Procédure `jobOffer.update` : même changement
5. Schéma Zod mis à jour : `location` retiré, `cityId` ajouté (`z.uuid().optional().nullable()`)
6. Détail offre (`/offers/[id]`) : affiche "Nom de la ville, Région" ou rien si `cityId` null
7. Liste offres : affiche la ville de l'offre si définie
8. Include `city: true` dans les queries Prisma `jobOffer.list` et `jobOffer.getById`
9. Tests : create offre avec `cityId` ; update `cityId → null` efface la ville

## Tasks / Subtasks

- [ ] Task 0 — Pré-requis
  - [ ] Vérifier que la story 5.3 est DONE (composant `CityAutocomplete` disponible)
  - [ ] Lire `src/server/trpc/routers/jobOffer.ts` en intégralité
  - [ ] Lire `src/lib/validations/jobOffer.ts` en intégralité
  - [ ] Lire le formulaire de création/édition offre et la page détail offre

- [ ] Task 1 (AC: 5) — Mettre à jour `src/lib/validations/jobOffer.ts`
  - [ ] Retirer `location: z.string().optional()...`
  - [ ] Ajouter `cityId: z.uuid().optional().nullable()`
  - [ ] Appliquer à `createJobOfferSchema` et `updateJobOfferSchema`

- [ ] Task 2 (AC: 3, 8) — Mettre à jour `jobOffer.create` dans le router
  - [ ] Retirer `location` de l'input et du `prisma.jobOffer.create`
  - [ ] Ajouter `cityId: input.cityId ?? null` dans `prisma.jobOffer.create`
  - [ ] Dans `jobOffer.list` : ajouter `include: { city: true }` (ou `select` avec `city: { select: { name, region, country } }`)
  - [ ] Dans `jobOffer.getById` : même ajout

- [ ] Task 3 (AC: 4) — Mettre à jour `jobOffer.update`
  - [ ] Retirer `location` de l'input et du `prisma.jobOffer.update`
  - [ ] Ajouter `cityId: input.cityId !== undefined ? input.cityId : undefined` (ne mettre à jour que si fourni)
  - [ ] Si `cityId` est explicitement `null` → efface la ville

- [ ] Task 4 (AC: 1) — Mettre à jour le formulaire de création offre
  - [ ] Localiser le champ `location` dans le formulaire React Hook Form
  - [ ] Remplacer par `<CityAutocomplete mode="single" value={selectedCity} onChange={setSelectedCity} />`
  - [ ] State local : `selectedCity: CityOption | null`
  - [ ] À la soumission : `cityId: selectedCity?.id ?? null`

- [ ] Task 5 (AC: 2) — Mettre à jour le formulaire d'édition offre
  - [ ] Pré-charger la ville depuis `jobOffer.getById` (relation `city`)
  - [ ] Mapper `{ id: offer.city.id, name: offer.city.name, region: offer.city.region, country: offer.city.country }` vers `CityOption`
  - [ ] Même logique de soumission que Task 4

- [ ] Task 6 (AC: 6, 7) — Mettre à jour l'affichage
  - [ ] Détail offre : remplacer `offer.location` par `offer.city ? `${offer.city.name}, ${offer.city.region}` : null`
  - [ ] Liste offres : remplacer `offer.location` par `offer.city?.name`

- [ ] Task 7 (AC: 9) — Tests
  - [ ] Test intégration `jobOffer.create` avec `cityId` → ville associée en base
  - [ ] Test `jobOffer.update` avec `cityId: null` → `cityId` null en base
  - [ ] Test `jobOffer.create` sans `cityId` → `cityId` null, pas d'erreur

## Dev Notes

### Previous Story Insights

- Story 5.1 : `JobOffer.location` n'existe plus en base. **Ne pas référencer `location` dans les requêtes Prisma.**
- Story 5.3 : `CityAutocomplete` disponible dans `src/components/shared/CityAutocomplete.tsx`

### Data Models

```
JobOffer {
  ...
  cityId    String?   (FK City, onDelete SetNull)
  city      City?     (relation)
  ...
}
```

La ville est optionnelle sur une offre (`cityId String?`). Une offre peut ne pas avoir de ville définie.

### API Specifications

**`jobOffer.create` input (extrait) :**
```ts
cityId: z.uuid().optional().nullable()  // undefined = non fourni ; null = explicitement sans ville
```

**`jobOffer.getById` output (extrait) :**
```ts
city: { id, name, region, country } | null
```

### File Locations

| Fichier | Action |
|---------|--------|
| `src/lib/validations/jobOffer.ts` | Modifié (location → cityId) |
| `src/server/trpc/routers/jobOffer.ts` | Modifié (create, update, list, getById) |
| Formulaire création offre | Modifié |
| Formulaire édition offre | Modifié |
| Page détail offre | Modifiée (affichage) |
| Page liste offres | Modifiée (affichage) |
| `tests/integration/jobOffer/jobOffer-city.test.ts` | Créé |

### Technical Constraints

- Invalider `utils.jobOffer.list.invalidate()` et `utils.jobOffer.getById.invalidate()` après les mutations
- La relation `city` est nullable — gérer le cas `city: null` dans tous les affichages

### Testing

- Tests d'intégration dans `tests/integration/jobOffer/` avec Prisma réel

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-14 | 0.1 | Story draft créée | John (PM) |

---

## Dev Agent Record

*(À remplir par l'agent de développement)*

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

---

## QA Results

*(À remplir par l'agent QA)*
