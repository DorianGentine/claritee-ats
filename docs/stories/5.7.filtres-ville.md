# Story 5.7: Filtres par ville — Candidats & Offres

## Status

done

## Story

**As a** recruiter,
**I want** to filter candidates and job offers by structured city,
**so that** I can narrow down results to specific locations.

## Acceptance Criteria

1. Filtre ville sur liste candidats : champ texte libre remplacé par `CityAutocomplete` (`mode="multi"`)
2. Procédure `candidate.list` : paramètre `city: string` remplacé par `cityIds: string[]`
3. Filtre `cityIds` en OR : `candidate.cities.some({ cityId: { in: cityIds } })`
4. Procédure `candidate.listCities` (renvoyait des strings distinctes) supprimée — plus utilisée
5. Filtre ville sur liste offres : champ texte libre remplacé par `CityAutocomplete` (`mode="single"`)
6. Procédure `jobOffer.list` : paramètre `location: string` remplacé par `cityId: string | null`
7. Filtre `jobOffer.list` par `cityId` : `WHERE JobOffer.cityId = cityId`
8. URL query params mis à jour : `cityIds` contient des UUIDs (pas des noms)
9. Chips actifs affichent le nom résolu de la ville (depuis le state local du filtre)
10. Tests : `candidate.list({ cityIds: [idParis] })` retourne uniquement les candidats avec Paris

## Tasks / Subtasks

- [x] Task 0 — Pré-requis
  - [x] Vérifier que les stories 5.3, 5.4 et 5.5 sont DONE
  - [x] Lire `src/server/trpc/routers/candidate.ts` — trouver la procédure `list` et `listCities`
  - [x] Lire `src/server/trpc/routers/jobOffer.ts` — trouver la procédure `list`
  - [x] Lire `src/app/(dashboard)/candidates/page.tsx` — comprendre comment les filtres sont gérés (state, URL params)
  - [x] Lire `src/app/(dashboard)/offers/page.tsx` — idem pour les offres

- [x] Task 1 (AC: 2, 3, 4) — Mettre à jour `candidate.list`
  - [x] Remplacer le paramètre `city: string` par `cityIds: z.array(z.uuid()).default([])`
  - [x] Remplacer la condition Prisma `{ city: { contains: city, mode: 'insensitive' } }` par :
    ```ts
    ...(cityIds.length > 0
      ? [{ cities: { some: { cityId: { in: cityIds } } } }]
      : [])
    ```
  - [x] Supprimer la procédure `candidate.listCities` du router (plus utilisée)
  - [x] Vérifier qu'aucun fichier n'importe encore `candidate.listCities` (grep `listCities`)

- [x] Task 2 (AC: 6, 7) — Mettre à jour `jobOffer.list`
  - [x] Remplacer le paramètre `location: string` par `cityId: z.uuid().optional().nullable()`
  - [x] Remplacer la condition Prisma `{ location: { contains: location, mode: 'insensitive' } }` par :
    ```ts
    ...(cityId ? [{ cityId }] : [])
    ```

- [x] Task 3 (AC: 1, 8, 9) — Mettre à jour le filtre ville sur la liste candidats
  - [x] Remplacer le champ texte `city` par `<CityAutocomplete mode="multi" value={selectedCities} onChange={setSelectedCities} />`
  - [x] State local du filtre : `selectedCities: CityOption[]`
  - [x] À la soumission du filtre ou au changement : passer `cityIds: selectedCities.map((city) => city.id)`
  - [x] URL query params : remplacer `?city=Paris` par `?cityIds=uuid1,uuid2` (encoder en CSV ou JSON selon la convention existante du projet)
  - [x] Chips actifs : afficher `city.name` (depuis `selectedCities` en state local) avec bouton × pour retirer

- [x] Task 4 (AC: 5, 8) — Mettre à jour le filtre ville sur la liste offres
  - [x] Remplacer le champ texte `location` par `<CityAutocomplete mode="single" value={selectedCity} onChange={setSelectedCity} />`
  - [x] À la soumission du filtre : passer `cityId: selectedCity?.id ?? null`
  - [x] URL query params : remplacer `?location=Paris` par `?cityId=uuid` (voir Completion Notes — n'existait pas)

- [x] Task 5 (AC: 10) — Tests
  - [x] Test intégration `candidate.list({ cityIds: [parisId] })` → retourne uniquement candidats avec Paris
  - [x] Test `candidate.list({ cityIds: [parisId, lyonId] })` → retourne candidats avec Paris OU Lyon (OR)
  - [x] Test `candidate.list({ cityIds: [] })` → retourne tous les candidats (pas de filtre)
  - [x] Test `jobOffer.list({ cityId: parisId })` → retourne uniquement offres à Paris
  - [x] Réactiver les tests `.todo` dans `__tests__/unit/lib/validations/offer.test.ts` (chemin réel — voir Completion Notes) :
    - `offerListInputSchema` : réécrits en tests `cityId` (valide/invalide)
  - [x] Réactiver les tests `.todo` dans `__tests__/db/server/trpc/routers/candidate.test.ts` (chemin réel) :
    - "list: filters by city" et "list: combines city and tagIds filters" → réécrits pour `cityIds`
    - "listDistinctCities" (2 tests) → supprimés (procédure déjà absente depuis la story 5.2)
    - Bonus : `.todo` supplémentaire trouvé dans `__tests__/unit/lib/validations/candidate.test.ts` ("filters by cities") → réécrit en tests `cityIds` (valide/invalide)
  - [x] Réactiver les tests `.todo` dans `__tests__/db/server/trpc/routers/offer.test.ts` (chemin réel) :
    - "filters by location (case-insensitive)" → réécrit en "filters by cityId"

### Review Findings

- [x] [Review][Patch] État dupliqué `cityIds`/`selectedCities` côté candidats : perte silencieuse de filtres + chip invisible sans bouton de retrait [src/app/(dashboard)/candidates/page.tsx, src/components/candidates/ActiveFilterChips.tsx, src/components/candidates/CandidateListFilters.tsx] — Au retour sur `/candidates?cityIds=...` (lien partagé ou navigation arrière), `selectedCities` redémarre vide alors que `filters.cityIds` (source de vérité pour le filtrage) contient déjà des UUIDs. Deux conséquences : (1) si l'utilisateur ajoute une ville via le picker, `cityIds` est recalculé uniquement à partir de `selectedCities.map(id)` — les villes déjà actives dans l'URL sont silencieusement effacées du filtre (perte de données, pas juste cosmétique) ; (2) tant qu'une ville de `filters.cityIds` n'est pas dans `selectedCities`, aucun chip ne s'affiche pour elle et elle est impossible à retirer individuellement (seul « Effacer les filtres » fonctionne). **Décision (Dorian, 2026-08-29) : option 1 — ajouter une procédure backend de résolution de noms de villes par ids** (ex. `city.getByIds` ou équivalent), et l'utiliser pour hydrater `selectedCities` au montage de `candidates/page.tsx` quand `filters.cityIds` (URL) contient des ids absents de `selectedCities`. Corrige à la fois la perte de données et l'affichage du chip.
- [x] [Review][Decision] Changements d'infra hors périmètre regroupés dans le diff de la story — `next-env.d.ts` (fichier généré, commentaire « ne pas éditer »), le nouveau hook `predev` dans `package.json`, et le nouveau script `scripts/check-local-services.sh` n'ont aucun rapport avec les filtres ville. **Résolu (Dorian, 2026-08-29) : gardé groupé dans ce commit, pas d'extraction.**

- [x] [Review][Patch] Double déclenchement de `onFiltersChange` sur chaque changement de ville [src/components/candidates/CandidateListFilters.tsx:112-117] — `onChange` du `CityAutocomplete` appelle `onSelectedCitiesChange` (qui appelle déjà `handleFiltersChange`) ET `onFiltersChange` directement, contrairement au pattern tags/langues (un seul appel). Retirer l'appel direct redondant.
- [x] [Review][Patch] Regex UUID côté client trop permissive pour `cityIds` [src/app/(dashboard)/candidates/page.tsx:25] — `UUID_RE = /^[0-9a-f-]{36}$/i` ne valide ni le format `8-4-4-4-12` ni les nibbles version/variant, contrairement au `z.uuid()` serveur. Un id malformé qui passe ce filtre côté client fait planter toute la requête `candidate.list`, sans état d'erreur gracieux (pattern préexistant pour `tagIds`, mais dupliqué frais ici pour `cityIds` sans être corrigé).
- [x] [Review][Patch] Nettoyage de test `cityIds` non scopé par ville [__tests__/db/server/trpc/routers/candidate.test.ts:212,313] — `db.candidateCity.deleteMany({ where: { candidateId: { in: [...] } } })` supprime toutes les lignes `CandidateCity` des deux candidats, sans filtrer par `cityId`, contrairement au test `tagIds` du même fichier qui scope son cleanup par `candidateId` ET `tagId`. Ajouter le filtre `cityId` pour rester cohérent.
- [x] [Review][Patch] `check-local-services.sh` : `set -e`/`pipefail` tue le script exactement dans le cas qu'il doit gérer [scripts/check-local-services.sh:19-20] — Vérifié par reproduction directe : si `NEXT_PUBLIC_SUPABASE_URL` est absent du fichier env, `grep` sort en erreur (1), `pipefail` propage ce statut à l'assignation `SUPABASE_URL=$(...)`, et `set -e` termine le script avant d'atteindre le `[[ -z "$SUPABASE_URL" ]] && exit 0` censé gérer ce cas silencieusement. Corrigé avec `|| true` sur l'assignation, revérifié par reproduction directe.
- [x] [Review][Patch] `check-local-services.sh` : détection « instance locale » par sous-chaîne naïve [scripts/check-local-services.sh:26-29] — `case "$SUPABASE_URL" in *127.0.0.1*|*localhost*)` matche n'importe où dans la chaîne, pas seulement le host. Une URL distante contenant « localhost » comme sous-chaîne serait à tort traitée comme locale. Corrigé avec un test ancré sur schéma+host (`[[ =~ ^https?://(127\.0\.0\.1|localhost)(:[0-9]+)?(/|$) ]]`).
- [x] [Review][Patch] `check-local-services.sh` : `curl` sans `--fail` [scripts/check-local-services.sh:32] — **Investigué, non appliqué : faux positif.** `curl -sf` a été testé contre le stack Supabase local réel (`docker ps` confirme tous les conteneurs "healthy") : Kong répond 404 sur la racine par design (routage par chemin, pas de route racine), donc `--fail` ferait échouer le check même quand Supabase est parfaitement sain — régression qui bloquerait `pnpm dev` à chaque session. Code laissé tel quel ; l'écart signalé est réel en théorie mais non actionnable pour ce endpoint précis.
- [x] [Review][Patch] Dev Notes `candidate.list.cityIds` à corriger, pas le code [Dev Notes → API Specifications] — Le code (`src/lib/validations/candidate.ts:13`) utilise `.optional()` + `.max(20, ...)`, pas `.default([])` comme documenté — mais c'est exactement le pattern déjà en place pour `tagIds` dans le même schéma, donc le code est cohérent avec l'existant. Mettre à jour les Dev Notes pour refléter la forme réelle.
- [x] [Review][Patch] Dev Notes `offer.list.cityId` à corriger, pas le code [Dev Notes → API Specifications] — Le code (`src/lib/validations/offer.ts:30`) utilise `z.uuid().optional()` sans `.nullable()` comme documenté — cohérent avec le champ sœur `clientCompanyId: z.uuid().optional()`, et aucun appelant n'envoie jamais `null` explicitement (`filters.city?.id` transforme déjà `null` en `undefined`). Mettre à jour les Dev Notes.
- [x] [Review][Patch] Incohérences internes du Dev Agent Record [File List + Completion Notes de cette story] — Le File List indique `sprint-status.yaml — 5-6 → done, 5-7 → in-progress` alors que le diff fixe bien `5-7-filtres-ville: review` ; les Completion Notes indiquent que `TO-DO.md` est « à mettre à jour séparément » alors que le diff le met déjà à jour. Corriger le texte. Bonus (mineur) : les Completion Notes ne mentionnent que candidate.list/offer.list/OfferListFilters comme « ajoutés, pas remplacés » — le filtre ville UI candidats non plus n'existait pas avant (vérifié : `CandidateListFilters.tsx`/`ActiveFilterChips.tsx` n'avaient aucun champ « Ville »), à ajouter pour la symétrie.

- [x] [Review][Defer] Filtrage défensif `.filter(Boolean).length` incohérent [src/server/trpc/routers/candidate.ts:114-119] — déjà présent pour `tagIds`/`languageNames`, `cityIds` suit juste le même pattern. — deferred, pre-existing
- [x] [Review][Defer] Cleanup de test sans `try/finally` [__tests__/db/server/trpc/routers/candidate.test.ts] — pattern déjà utilisé par tous les tests du fichier (ex: test `tagIds` ligne ~264), pas propre à `cityIds`. — deferred, pre-existing
- [x] [Review][Defer] Risque de collision `Date.now()` sur noms de fixtures créées en `Promise.all` — même pattern déjà utilisé pour les fixtures `tags` du même fichier. — deferred, pre-existing
- [x] [Review][Defer] Pas de dédoublonnage des `cityIds` parsés depuis l'URL avant le `.slice(0, 20)` [src/app/(dashboard)/candidates/page.tsx] — même lacune déjà présente pour `tagIds`. — deferred, pre-existing

### Review Findings — round 2 (2026-08-30)

Portée : re-review après le fix `showChips`/`FilterChipsBar`/`getTriggerLabel` ajouté suite aux retours utilisateur.

- [x] [Review][Patch] Fenêtre de course résiduelle dans `handleCitiesChange` [src/app/(dashboard)/candidates/page.tsx] — Le fix `city.getByIds` de la review round 1 ne ferme la perte de données que si l'hydratation se termine avant la première interaction utilisateur. Si l'utilisateur arrive sur `/candidates?cityIds=X,Y` et sélectionne une nouvelle ville avant que `city.getByIds` ait résolu, `cities` (callback du picker) ne contient que la nouvelle sélection — `X` et `Y` sont à nouveau silencieusement effacés. `handleCitiesChange` doit fusionner avec les ids encore non résolus (`missingCityIds`) plutôt que de faire confiance uniquement à `selectedCities`.
- [x] [Review][Patch] Chip ville invisible et irretirable si `city.getByIds` échoue ou ne résout jamais [src/components/candidates/ActiveFilterChips.tsx] — `flatMap` avec `if (!city) return []` fait disparaître silencieusement tout id non résolu (pas seulement pour une ville supprimée — vérifié : aucune procédure de suppression de ville n'existe dans l'app — mais aussi pour un simple raté réseau sur `city.getByIds`, sans retry). Remplacé par un chip de repli (label générique « Ville : résolution… » + bouton retirer fonctionnel) plutôt que de ne rien rendre.
- [x] [Review][Patch] `ordered={false}` devenu mort sur le `CityAutocomplete` de `CandidateListFilters` [src/components/candidates/CandidateListFilters.tsx] — `ordered` n'a d'effet que dans la liste de chips interne, qui ne se rend jamais tant que `showChips={false}`. Prop vestigiale retirée.
- [x] [Review][Patch] Débordement visuel possible du libellé du déclencheur pour un nom de ville long [src/components/shared/CityAutocomplete.tsx] — quand `showChips=false` (mode single), `getTriggerLabel` renvoie `props.value.name` rendu en texte brut sans `truncate`, contrairement aux autres affichages de nom de ville du composant (pill mode single, chips mode multi) qui utilisent tous `truncate`. Ajout de `truncate` sur le libellé du déclencheur.
- [x] [Review][Patch] Indentation incohérente dans `parseFiltersFromSearchParams` [src/app/(dashboard)/candidates/page.tsx] — **corrigé manuellement (pas via `pnpm format`)** : un premier essai avec `prettier --write` a bien corrigé l'indentation mais a aussi ajouté des points-virgules sur tout le fichier (ce fichier, comme 125 autres du repo, n'a jamais été passé au formatter malgré `semi: true` dans `.prettierrc` — dette de formatage préexistante et hors périmètre). Reconstruction manuelle du fichier pour ne corriger que l'indentation des 3 blocs ternaires, sans toucher au reste (diff ramené de 203 à 36 lignes changées).
- [x] [Review][Patch] Complétion Notes : convention `MultiSelectPopover` invoquée à tort [Completion Notes, entrée 0.4] — le texte affirme que le libellé multi de `getTriggerLabel` ("N ville(s) sélectionnée(s)") suit "la même convention que MultiSelectPopover pour tags/langues/statuts", mais `MultiSelectPopover` utilise un suffixe littéral non conjugué (`` `${n} tag(s) sélectionné(s)` ``) alors que `getTriggerLabel` fait un vrai accord grammatical (`` `${count} ville${count > 1 ? "s" : ""}...` ``). Note corrigée.
- [x] [Review][Patch] Raison du `curl` sans `--fail` non documentée dans le script [scripts/check-local-services.sh] — l'omission est délibérée (Kong répond 404 sur `/` par design, cf. investigation round 1) mais rien dans le script ne le dit. Commentaire ajouté.
- [x] [Review][Patch] Zéro couverture de test sur tout le code du round 2 — aucun test n'exerce `showChips`/`getTriggerLabel` dans `CityAutocomplete.test.tsx` ; aucun fichier de test n'existe pour `ActiveFilterChips.tsx` (candidats) ni pour le nouveau `FilterChipsBar.tsx`. Ajout de 5 tests `showChips` dans `CityAutocomplete.test.tsx` (single + multi, avec/sans sélection, accord singulier/pluriel) et d'un nouveau fichier `__tests__/unit/components/candidates/ActiveFilterChips.test.tsx` (7 tests : tags, langues, ville résolue, chip de repli non résolu, retrait, compteur/chargement). `FilterChipsBar` reste couvert indirectement via les deux consommateurs (candidats + offres) — un test dédié serait redondant vu sa taille (pas de logique propre au-delà du rendu de la liste).

- [x] [Review][Defer] Pas de dédoublonnage sur `cityGetByIdsInputSchema` — même lacune que le `cityIds` côté URL, déjà déférée round 1. — deferred, pre-existing pattern
- [x] [Review][Defer] Logique de pluralisation dupliquée trois fois (`getTriggerLabel`, `ActiveFilterChips`, `ActiveOfferFilterChips`) — code-smell mineur, pas de bug ; extraire un helper commun serait de la sur-ingénierie pour trois occurrences de deux lignes chacune. — deferred
- [x] [Review][Defer] `showChips={false}` en mode single n'a pas de contrat renforcé au niveau des types pour garantir qu'un parent fournit bien un retrait externe — seuls 2 call sites existent aujourd'hui, tous deux corrects. — deferred
- [x] [Review][Defer] `check-local-services.sh` : un commentaire en fin de ligne dans `.env` (`URL=... # local`) serait capturé dans la valeur et ferait échouer silencieusement la détection "instance locale" — hypothétique, les `.env`/`.env.local` réels du projet n'utilisent pas ce style. — deferred

**Écarté comme bruit :** `city.getByIds` en `publicProcedure` sans scoping `companyId` — vérifié contre le schéma Prisma : `City` n'a pas de colonne `companyId` (donnée de référence globale), même frontière de confiance que `city.autocomplete` déjà en place. Pas une fuite multi-tenant.

## Dev Notes

### Previous Story Insights

- Stories 5.4 et 5.5 terminées : `candidate.cities` (relation) et `jobOffer.cityId` (FK) sont en place
- La procédure `candidate.listCities` existait pour alimenter l'ancien filtre texte — elle est supprimée ici. Vérifier qu'aucun composant frontend ne l'appelle encore avant de la supprimer.
- Le filtre existant sur `candidate.list` utilisait `city: string` avec `contains + mode: 'insensitive'` — remplacer par la logique `cityIds` OR.

### API Specifications

**`candidate.list` input (extrait) :**
```ts
// Avant
city: z.string().optional()
// Après (aligné sur le pattern déjà en place pour tagIds dans le même schéma)
cityIds: z.array(z.uuid()).max(20, "Maximum 20 villes pour le filtre").optional()
```

**Logique OR pour les candidats :**  
Un candidat est inclus s'il a **au moins une** de ses villes dans `cityIds`. Cela correspond à `some` dans Prisma.

**`jobOffer.list` input (extrait) :**
```ts
// Avant
location: z.string().optional()
// Après (aligné sur le pattern déjà en place pour clientCompanyId dans le même schéma ;
// pas de .nullable() car aucun appelant n'envoie jamais null explicitement)
cityId: z.uuid().optional()
```

### File Locations

| Fichier | Action |
|---------|--------|
| `src/server/trpc/routers/candidate.ts` | Modifié (list : city→cityIds, suppression listCities) |
| `src/server/trpc/routers/jobOffer.ts` | Modifié (list : location→cityId) |
| `src/app/(dashboard)/candidates/page.tsx` | Modifié (filtre UI) |
| `src/app/(dashboard)/offers/page.tsx` | Modifié (filtre UI) |
| `tests/integration/candidate/candidate-filters.test.ts` | Créé ou modifié |

### Technical Constraints

- `cityIds` dans les URL params : si le projet encode déjà les arrays en CSV (`?tags=id1,id2`), suivre la même convention. Sinon utiliser `?cityIds[]=id1&cityIds[]=id2`.
- Les chips actifs doivent afficher le **nom** de la ville (pas l'UUID). Ces noms viennent du state local `selectedCities` — pas besoin d'un appel supplémentaire.
- Invalider `utils.candidate.list.invalidate()` et `utils.jobOffer.list.invalidate()` si nécessaire lors du changement de filtre.

### Testing

- Tests d'intégration dans `tests/integration/` avec Prisma réel
- Vérifier la logique OR pour le filtre multi-villes candidats

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-14 | 0.1 | Story draft créée | John (PM) |
| 2026-08-29 | 0.2 | Implémentation complète (filtres ville candidats + offres) | Amelia (Dev Agent) |
| 2026-08-29 | 0.3 | Review findings : `city.getByIds` (résolution nom de ville depuis l'URL) | Amelia (Dev Agent) |
| 2026-08-29 | 0.4 | Fix doublon de chips ville (`showChips` sur `CityAutocomplete`) | Amelia (Dev Agent) |
| 2026-08-29 | 0.5 | Extraction partielle `FilterChipsBar` (dédup `ActiveFilterChips`/`ActiveOfferFilterChips`) | Amelia (Dev Agent) |
| 2026-08-29 | 0.6 | Extraction `getTriggerLabel` dans `CityAutocomplete` (lisibilité) | Amelia (Dev Agent) |
| 2026-08-30 | 0.7 | Review round 2 : fenêtre de course résiduelle, chip de repli, tests `showChips`/`ActiveFilterChips` | Amelia (Dev Agent) |

---

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5), persona Amelia — bmad-dev-story

### Debug Log References

Aucun. `pnpm lint`, `npx tsc --noEmit`, `pnpm test:unit` (356 tests) et `pnpm test:db` (215 tests, 14 skip pré-existants — dette connue documentée dans `docs/prd/TO-DO.md`) tous verts.

**Après traitement des Review Findings (2026-08-29)** : `pnpm lint`, `npx tsc --noEmit`, `pnpm test:unit` (356 tests) et `pnpm test:db` (218 tests, +3 pour `city.getByIds` ; 14 skip pré-existants inchangés) tous verts. Script `check-local-services.sh` revérifié en conditions réelles contre le stack Supabase local (`docker ps` — tous conteneurs healthy).

**Après retour utilisateur — doublon de chips (2026-08-29)** : `pnpm lint`, `npx tsc --noEmit`, `pnpm test:unit` (356 tests, inchangé) tous verts.

**Après retour utilisateur — fusion partielle `ActiveFilterChips`/`ActiveOfferFilterChips` (2026-08-29)** : `pnpm lint`, `npx tsc --noEmit`, `pnpm test:unit` (356 tests, inchangé — `ActiveOfferFilterChips.test.tsx` passe sans modification, la sortie rendue étant identique) tous verts.

**Après retour utilisateur — extraction `getTriggerLabel` (2026-08-29)** : `pnpm lint`, `npx tsc --noEmit`, `pnpm test:unit` (356 tests, inchangé) tous verts.

**Après review round 2 (2026-08-30)** : `pnpm lint`, `npx tsc --noEmit`, `pnpm test:unit` (368 tests, +12 : 5 `showChips` dans `CityAutocomplete.test.tsx`, 7 dans le nouveau `ActiveFilterChips.test.tsx` candidats) et `pnpm test:db` (218 tests, inchangé) tous verts.

### Completion Notes List

- **Écart important entre les hypothèses de la story et l'état réel du code au moment du dev.** La story décrit un remplacement d'un filtre texte existant. En réalité :
  - `candidate.list` n'avait **aucun** filtre ville (ni `city: string`, ni la procédure `listCities` — déjà supprimée depuis la story 5.2). Le filtre `cityIds` a donc été **ajouté**, pas remplacé.
  - `offer.list` (le router s'appelle `offer`, pas `jobOffer` — `offerRouter` dans `src/server/trpc/routers/offer.ts`) n'avait **aucun** filtre ville/`location` côté backend. Le filtre `cityId` a été **ajouté**.
  - Côté UI offres, un champ texte "Ville" (`location`) existait bien dans `OfferListFilters`/`ActiveOfferFilterChips`, mais **n'était jamais transmis** à `api.offer.list.useQuery` dans `offers/page.tsx` (filtre mort, sans effet sur les résultats). Corrigé au passage en le remplaçant par `CityAutocomplete` (mode single) correctement branché sur `cityId`.
  - Côté UI candidats aussi : `CandidateListFilters`/`ActiveFilterChips` n'avaient aucun champ "Ville" (texte ou autre) avant ce diff. Le filtre `CityAutocomplete` (mode multi) a donc été **ajouté**, pas remplacé, symétriquement au backend.
- **Chemins de tests réels** (différents de ceux indiqués dans la story, qui datent d'avant la réorganisation de l'arborescence de tests) : `__tests__/unit/lib/validations/`, `__tests__/db/server/trpc/routers/` — pas `tests/unit/` ni `tests/integration/`. Voir aussi `project-context.md`.
- **Pas de synchronisation URL pour les filtres offres** : contrairement à `candidates/page.tsx`, `offers/page.tsx` gère ses filtres en state local pur (pas de `useSearchParams`/`router.replace`). Le filtre ville suit donc le même pattern que les filtres existants (statut, tags, salaire, client) — pas de migration `?location=` → `?cityId=` car aucun paramètre URL n'existait pour ce filtre.
- **Limitation initiale sur les chips "Ville" côté candidats, corrigée en review** : le nom affiché venait uniquement du state local `selectedCities`, sans hydratation depuis l'URL — voir Review Findings ci-dessus (résolu par l'ajout de `city.getByIds`).
- **Effet de bord (hors périmètre strict mais bloquant sinon)** : la story 5.6 (entreprise cliente — villes) était déjà **DONE** (mergée sur `main`, story file à jour) mais `docs/implementation-artifacts/sprint-status.yaml` et `docs/prd/TO-DO.md` n'avaient pas été mis à jour en conséquence. Corrigé (sprint-status.yaml et TO-DO.md mis à jour dans ce diff).
- **Vérification navigateur** : serveur `pnpm dev` démarré ; `/candidates` s'est chargé sans erreur avec le nouveau paramètre `cityIds` (session authentifiée déjà active dans un navigateur connecté en local, requêtes tRPC `candidate.list`/`tag.list`/`candidate.listDistinctLanguageNames` en succès). Le clic-à-clic sur le nouveau sélecteur "Ville" n'a pas été vérifié interactivement par l'agent (pas d'outil d'automatisation navigateur/session authentifiée scriptable disponible) — à confirmer visuellement sur `/candidates` et `/offers`.
- **Doublon de chips signalé par Dorian (2026-08-29)** : sur `/candidates` (mode multi) et `/offers` (mode single), `CityAutocomplete` affichait sa propre chip de sélection en plus de celle déjà rendue par `ActiveFilterChips`/`ActiveOfferFilterChips` — deux affordances de suppression pour le même filtre. Ajout d'une prop `showChips?: boolean` (défaut `true`, donc **aucun changement** pour les 3 autres consommateurs du composant — `CandidateCitiesField`, `ClientCompanyForm`, `JobOfferForm` — qui restent en formulaire avec chips intégrées). Passée à `false` uniquement dans `CandidateListFilters` et `OfferListFilters` : le déclencheur affiche alors lui-même l'état sélectionné (nom unique en mode single, décompte "N ville(s) sélectionnée(s)" en mode multi), sans chip ni bouton × interne ; la suppression du filtre passe uniquement par la chip externe. Correction (round 2) : le décompte fait un vrai accord grammatical ("1 ville sélectionnée" / "2 villes sélectionnées") — ce n'est pas la même convention que `MultiSelectPopover`, qui utilise un suffixe littéral non conjugué (`"N tag(s) sélectionné(s)"`) ; l'affirmation initiale de "même convention" était incorrecte, sans impact fonctionnel.
- **Fusion partielle `ActiveFilterChips`/`ActiveOfferFilterChips` demandée par Dorian (2026-08-29)** : les deux composants dupliquaient le wrapper (`role="list"`, gap, texte de comptage) et le rendu individuel des chips — mais portent une logique métier différente (candidats : tags/villes/langues ; offres : statuts/tags/salaire/ville/client). Fusion complète écartée (aurait créé un composant connaissant les deux domaines) au profit d'une extraction partielle : `src/components/shared/FilterChipsBar.tsx` prend une liste `{ key, label, onRemove, removeAriaLabel }[]` + un `countLabel` déjà formaté, et rend le wrapper + les chips + le texte de comptage/chargement. `ActiveFilterChips` et `ActiveOfferFilterChips` restent les seuls points qui connaissent leurs filtres respectifs ; ils construisent la liste de chips puis délèguent le rendu. Le composant partagé ne décide pas seul de s'afficher (pas de `return null` interne) — chaque appelant garde son propre garde `hasActiveXFilters`, pour ne pas faire disparaître tout le bandeau (texte de comptage inclus) pendant qu'un chip est encore en cours de résolution (ex. nom de ville via `city.getByIds`).

### File List

- `src/lib/validations/candidate.ts` — ajout `cityIds` à `candidateListInputSchema`
- `src/lib/validations/offer.ts` — ajout `cityId` à `offerListInputSchema`
- `src/server/trpc/routers/candidate.ts` — filtre `cityIds` (OR via `cities.some`)
- `src/server/trpc/routers/offer.ts` — filtre `cityId`
- `src/components/candidates/CandidateListFilters.tsx` — `CityAutocomplete` (mode multi), `cityIds` dans `CandidateFilters`
- `src/components/candidates/ActiveFilterChips.tsx` — chips ville (résolution via `selectedCities`)
- `src/app/(dashboard)/candidates/page.tsx` — state `selectedCities`, sync URL `cityIds`
- `src/components/offers/OfferListFilters.tsx` — `CityAutocomplete` (mode single) à la place du champ texte `location`
- `src/components/offers/ActiveOfferFilterChips.tsx` — chip ville (`city: CityOption | null`)
- `src/app/(dashboard)/offers/page.tsx` — `cityId` branché sur la query, `onRemoveCity`
- `__tests__/unit/lib/validations/candidate.test.ts` — tests `cityIds`
- `__tests__/unit/lib/validations/offer.test.ts` — tests `cityId`
- `__tests__/db/server/trpc/routers/candidate.test.ts` — tests filtre `cityIds` (simple, OR, combiné avec tagIds), suppression des `.todo` `listDistinctCities`
- `__tests__/db/server/trpc/routers/offer.test.ts` — test filtre `cityId`
- `__tests__/unit/components/offers/OfferListFilters.test.tsx` — mock `city.autocomplete`
- `__tests__/unit/components/offers/ActiveOfferFilterChips.test.tsx` — `city` au lieu de `location`
- `docs/implementation-artifacts/sprint-status.yaml` — 5-6 → done, 5-7 → review
- `docs/prd/TO-DO.md` — 5.6 → DONE, 5.7 → review

**Ajoutés lors du traitement des Review Findings (2026-08-29) :**

- `src/lib/validations/city.ts` — ajout `cityGetByIdsInputSchema`
- `src/server/trpc/routers/city.ts` — nouvelle procédure `getByIds` (résolution de villes par ids)
- `src/app/(dashboard)/candidates/page.tsx` — hydratation de `selectedCities` via `city.getByIds`, regex `UUID_RE` corrigée
- `src/components/candidates/CandidateListFilters.tsx` — suppression du double appel `onFiltersChange`
- `__tests__/db/server/trpc/routers/candidate.test.ts` — cleanup `candidateCity.deleteMany` scopé par `cityId`
- `__tests__/db/server/trpc/routers/city.test.ts` — tests `city.getByIds` (vide, résolution par id, id inexistant)
- `scripts/check-local-services.sh` — fix `set -e`/`pipefail` sur l'absence de variable, détection "instance locale" ancrée sur le host

**Ajoutés suite au retour utilisateur — dédoublonnage des chips (2026-08-29) :**

- `src/components/shared/CityAutocomplete.tsx` — prop `showChips` (défaut `true`) ; masque la chip/liste de chips interne et affiche l'état sélectionné directement dans le déclencheur quand désactivée
- `src/components/candidates/CandidateListFilters.tsx` — `showChips={false}` sur le `CityAutocomplete` du filtre ville
- `src/components/offers/OfferListFilters.tsx` — `showChips={false}` sur le `CityAutocomplete` du filtre ville

**Ajoutés suite au retour utilisateur — fusion partielle `ActiveFilterChips`/`ActiveOfferFilterChips` (2026-08-29) :**

- `src/components/shared/FilterChipsBar.tsx` — nouveau composant partagé (wrapper + rendu des chips + texte de comptage)
- `src/components/candidates/ActiveFilterChips.tsx` — construit la liste de chips (tags/villes/langues) et délègue le rendu à `FilterChipsBar`
- `src/components/offers/ActiveOfferFilterChips.tsx` — construit la liste de chips (statuts/tags/salaire/ville/client) et délègue le rendu à `FilterChipsBar`

**Ajouté suite au retour utilisateur — lisibilité (2026-08-29) :**

- `src/components/shared/CityAutocomplete.tsx` — extraction de `getTriggerLabel(props, showChips)` au-dessus du composant (remplace les ternaires imbriqués calculant le libellé du déclencheur)

**Ajoutés lors du traitement des Review Findings round 2 (2026-08-30) :**

- `src/app/(dashboard)/candidates/page.tsx` — `handleCitiesChange` fusionne avec `missingCityIds` (fix fenêtre de course), indentation de `parseFiltersFromSearchParams` corrigée manuellement
- `src/components/candidates/ActiveFilterChips.tsx` — chip de repli pour ville non résolue (au lieu de disparaître)
- `src/components/candidates/CandidateListFilters.tsx` — retrait de la prop `ordered={false}` devenue morte
- `src/components/shared/CityAutocomplete.tsx` — `truncate` sur le libellé du déclencheur
- `scripts/check-local-services.sh` — commentaire expliquant l'absence volontaire de `--fail` sur `curl`
- `__tests__/unit/components/shared/CityAutocomplete.test.tsx` — 5 tests `showChips`/`getTriggerLabel`
- `__tests__/unit/components/candidates/ActiveFilterChips.test.tsx` — nouveau fichier (7 tests)

---

## QA Results

*(À remplir par l'agent QA)*
