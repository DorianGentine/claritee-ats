# Story 3.8: Candidature Status Management

## Status

READY

## Story

**As a** recruiter,  
**I want** to update the status of a candidate for a specific offer,  
**so that** I can track their progress in the pipeline.

## Acceptance Criteria

1. Sur la fiche offre `/offers/[id]`, chaque ligne de candidature (section Candidats) affiche un **dropdown de statut** permettant de changer le statut de la candidature  
2. Les statuts disponibles sont exactement : "Contacté sur LinkedIn", "Contact téléphonique", "Postulé", "Accepté", "Refusé par l'employeur", "Rejeté par le candidat"  
3. Tout changement de statut est **enregistré immédiatement** (optimistic UI) et reflété dans la section Candidats sans rechargement complet de la page  
4. Le changement de statut met à jour le champ `updatedAt` de la candidature  
5. Les statuts sont affichés sous forme de **badges de couleur** (fond + texte) permettant de distinguer facilement les états dans la liste (Design System §2)  
6. L’historique complet des statuts n’est **pas** stocké en MVP (seul le statut courant est conservé)  
7. Le statut de chaque candidature est également visible sur la fiche candidat `/candidates/[id]`, par offre associée  
8. Il est possible de **dissocier** un candidat d’une offre (suppression de la candidature) depuis la fiche offre, avec confirmation utilisateur  
9. Un même candidat peut avoir des statuts différents sur des offres différentes (gestion par candidature, pas par candidat globalement)  
10. La section Candidats de la fiche offre permet de **filtrer les candidatures par statut** (filtre simple) pour faciliter la lecture du pipeline

**Réf.** PRD § Epic 3, Story 3.8 ; Design System §2 « Statuts candidature » (6 valeurs avec couleurs fond/texte) ; Wireframes §5 (dropdown statut par candidat, bouton Dissocier).

> Note :  
> - La création des candidatures et la visibilité de base (association candidat–offre, statut initial par défaut) sont couvertes par la **Story 3.7**.  
> - Cette story 3.8 se concentre sur la **mise à jour du statut**, la **dissociation** et les **filtres par statut**, en réutilisant les composants et mappings créés en Stories 3.6 et 3.7.

## Tasks / Subtasks

- [ ] Task 0 – Standards & architecture (pré‑requis)  
  - [ ] Avant de commencer l’implémentation de cette story, lire (ou relire) les documents suivants et s’assurer que les décisions de code et de test y sont conformes :
    - `docs/architecture/coding-standards.md`
    - `docs/frontend-architecture.md`
    - `docs/architecture.md`
  - [ ] Adapter au besoin les tâches ci‑dessous pour respecter les patterns recommandés (structure des routers tRPC, conventions frontend, stratégie de tests, a11y, DRY, etc.).

- [ ] Task 1 (AC: 2, 4, 5, 6, 9) – Modèle & API de mise à jour du statut

  - [ ] Vérifier que l’enum `CandidatureStatus` couvre exactement les 6 valeurs prévues par le Design System (statuts FR ci‑dessus) et que le mapping UI `candidature-status-style.ts` est complet.  
  - [ ] Dans `src/server/trpc/routers/offer.ts` (ou route dédiée `candidature.ts` si existante), ajouter une mutation tRPC pour mettre à jour le statut d’une candidature, par ex. `candidature.updateStatus` :
    - [ ] Input Zod (ex. dans `src/lib/validations/offer.ts` ou fichier dédié) :
      - [ ] `{ candidatureId: z.uuid(), status: z.nativeEnum(CandidatureStatus) }` ou équivalent.  
    - [ ] Utiliser `protectedProcedure` et vérifier que la candidature appartient bien au `companyId` courant (via relations Candidate/JobOffer).  
    - [ ] Mettre à jour uniquement le champ `status` (et laisser Prisma gérer `updatedAt` ou le mettre à jour explicitement selon le modèle).  
  - [ ] S’assurer que la mutation ne modifie pas `createdAt` ni d’autres champs, et qu’aucun historique n’est créé (MVP).  
  - [ ] Vérifier que les queries qui retournent les candidatures (ex. `offer.getById`, `candidate.getById`) renvoient bien le **statut courant** et le `updatedAt` à jour après un changement de statut.

- [ ] Task 2 (AC: 1, 2, 3, 5, 10) – UI dropdown statut + filtre par statut sur la fiche offre

  - [ ] Dans la section Candidats de la fiche offre (`OfferCandidatesSection`), remplacer le badge de statut **statique** (Story 3.6) par un **composant interactif** combinant badge et dropdown :
    - [ ] Afficher le statut courant sous forme de badge (en réutilisant `candidature-status-style.ts` pour les couleurs et labels).  
    - [ ] Au clic sur le badge (ou une icône dédiée), ouvrir un menu déroulant listant les 6 statuts disponibles.  
    - [ ] Le composant doit être accessible (navigable au clavier, aria‑labels corrects), en s’appuyant sur les composants Radix/shadcn (ex. `DropdownMenu`).  
  - [ ] Au changement de statut dans le menu :
    - [ ] Déclencher la mutation tRPC de mise à jour (`candidature.updateStatus`).  
    - [ ] Implémenter une **optimistic UI** : mettre à jour immédiatement l’état local de la candidature avec le nouveau statut, puis :
      - [ ] En cas de succès, laisser la valeur.  
      - [ ] En cas d’erreur, revert à l’ancien statut et afficher un `toast.error` avec un message générique.  
  - [ ] Vérifier que :
    - [ ] Le `updatedAt` est mis à jour côté back et reflété dans les données rechargées (au besoin, invalider les queries `offer.getById` / `candidate.getById` après mutation réussie, ou mettre à jour le cache manuellement).  
    - [ ] Les compteurs par statut (Story 3.6) sont recalculés/cohérents après changement de statut (via invalidation ou mise à jour du cache).  
  - [ ] Ajouter un **filtre par statut** en haut de la section Candidats :
    - [ ] Contrôle simple (ex. select ou pill buttons) permettant de filtrer la liste des candidatures affichées par statut.  
    - [ ] Valeur "Tous" par défaut ; filtrage réalisé côté front sur la liste déjà chargée (pas nécessairement une query dédiée pour le MVP).  
    - [ ] Le filtre doit fonctionner avec les changements de statut (la ligne peut disparaître de la vue si son nouveau statut ne matche plus le filtre actif).

- [ ] Task 3 (AC: 7, 9) – Visibilité et mise à jour du statut sur la fiche candidat

  - [ ] Étendre la section "Offres associées" / "Candidatures" de la fiche candidat (`/candidates/[id]`, composant `CandidateCandidaturesSection` ou équivalent) pour :
    - [ ] Afficher, pour chaque offre associée, le **statut courant** de la candidature sous forme de badge coloré (réutilisation de `candidature-status-style.ts`).  
    - [ ] Optionnel (selon UX décidée) : rendre le badge interactif ici aussi (dropdown de statut) ou, si l’on préfère limiter les changements à la fiche offre en MVP, afficher uniquement un badge non modifiable mais à jour.  
  - [ ] S’assurer que la fiche candidat reflète bien les changements de statut effectués côté fiche offre (utiliser les mêmes sources de données, invalidation TanStack Query appropriée).  
  - [ ] Gérer les états `loading` / `error` de cette section de manière cohérente avec le reste de l’UI.

- [ ] Task 4 (AC: 8, 9) – Dissociation (suppression de candidature)

  - [ ] Ajouter un **bouton "Dissocier"** sur chaque ligne de candidature dans la section Candidats de la fiche offre (conformément aux wireframes) :
    - [ ] Icône et label conformes au Design System (bouton texte ou icône avec tooltip).  
    - [ ] Clic ouvre un dialogue de **confirmation** ("Confirmer la dissociation de ce candidat de cette offre ?").  
  - [ ] Côté tRPC, ajouter une mutation pour supprimer une candidature (ex. `candidature.delete`) :
    - [ ] Input : `{ candidatureId: z.uuid() }`.  
    - [ ] `protectedProcedure` avec vérification du `companyId`.  
    - [ ] Suppression de la ligne `Candidature` correspondante.  
  - [ ] Côté UI :
    - [ ] En cas de confirmation, appeler la mutation de suppression.  
    - [ ] Mettre à jour la liste locale (ou invalider `offer.getById` et `candidate.getById`), mettre à jour les compteurs par statut, et afficher un `toast.success` générique.  
    - [ ] En cas d’erreur, afficher un `toast.error` générique.  
  - [ ] Vérifier que la suppression n’impacte que la candidature ciblée (le candidat reste existant, l’offre reste existante) et que les autres candidatures de ce candidat sur d’autres offres ne sont pas affectées.

- [ ] Task 5 – Testing (AC: 1–10)

  - [ ] **Report Story 3.7** — Tests de composants React non couverts en 3.7 (environment vitest `node` sans jsdom). Les implémenter ici et, une fois faits, cocher les sous-tâches correspondantes dans `docs/stories/3.7.candidate-offer-association.md` (Task 5) :
    - [ ] `CandidateSelectDialog` : rendu avec liste initiale, filtrage par nom/titre, sélection multiple, appel de la mutation `offer.addCandidates`.
    - [ ] `CandidateCandidaturesSection` : état vide, rendu avec au moins une candidature, lien cliquable vers `/offers/[id]`.
    - [ ] `OfferCandidatesSection` après mutation réussie : vérifier que les nouvelles candidatures sont reflétées (via mocks de hooks tRPC et invalidation simulée).

  - [ ] Tests d’intégration back (Vitest) :
    - [ ] Couvrir la mutation de mise à jour de statut :
      - [ ] Changement de statut valide sur une candidature du `companyId` courant.  
      - [ ] Mise à jour correcte de `updatedAt`.  
      - [ ] Erreur si la candidature n’appartient pas au `companyId` courant (multi‑tenancy).  
    - [ ] Couvrir la mutation de suppression de candidature :
      - [ ] Suppression réussie d’une candidature valide.  
      - [ ] Erreur / NOT_FOUND ou UNAUTHORIZED si la candidature n’appartient pas au `companyId` courant.  
  - [ ] Tests unitaires :
    - [ ] Schémas Zod d’input pour `candidature.updateStatus` et `candidature.delete`.  
    - [ ] Vérifier que `candidature-status-style.ts` couvre bien les 6 statuts définis dans le PRD (labels, couleurs).  
  - [ ] Tests de composants (au moins smoke tests) :
    - [ ] Composant de dropdown de statut sur la fiche offre : rendu des 6 options, changement de valeur, appel de callback/mutation, gestion optimistic UI (peut être simulée).  
    - [ ] Filtre par statut : affichage correct des candidatures selon le statut sélectionné, retour à "Tous".  
    - [ ] Bouton Dissocier : affichage du dialogue de confirmation, appel de la mutation, mise à jour de la liste (peut être mockée).  
    - [ ] Section "Candidatures" sur la fiche candidat : statut correct affiché par offre associée après changement côté fiche offre (via mocks de hooks tRPC).

- [ ] Task 6 (QA) – Cas de test

  - [ ] Rédiger les cas de test QA détaillés dans un fichier dédié `docs/stories/cases/3.8-test-cases.md` (scénarios : changement de statut simple, multiples changements successifs, dissociation, multi‑tenancy, cohérence offre ↔ candidat, filtrage par statut, comportements d’erreur) pour faciliter la review de la story.

- [ ] Task 7 (review) – Code review

  - [ ] Lancer une review de code en fin de développement avant clôture de la story (revue des mutations tRPC, du dropdown de statut, du bouton Dissocier, des filtres et des tests).

## Dev Notes

### Dependencies & Previous Stories

- **Story 3.6** : fiche offre détaillée `/offers/[id]`, section Candidats avec compteurs par statut et badge statique de statut (préparation pour 3.7/3.8).  
- **Story 3.7** : création et visibilité des candidatures (association candidat–offre, statut initial `Contacté sur LinkedIn`, section "Offres associées" sur la fiche candidat).  
- Cette Story 3.8 s’appuie sur les modèles et associations mis en place en 3.7 et 3.6, en ajoutant la gestion dynamique du statut, la dissociation et les filtres par statut.

### Data Models

- **Candidature** (rappel) :  
  - `id` (UUID)  
  - `candidateId` (FK `Candidate`)  
  - `offerId` (FK `JobOffer`)  
  - `status` (`CandidatureStatus`) – 6 valeurs définies dans le Design System  
  - `createdAt`, `updatedAt`  
- Aucune table d’historique n’est ajoutée dans cette story (MVP) ; seul l’état courant est stocké.

### API Specifications

- **candidature.updateStatus** (nom à confirmer) :
  - Input : `{ candidatureId: z.uuid(), status: z.nativeEnum(CandidatureStatus) }`.  
  - Comportement :
    - Vérifier que la candidature appartient au `companyId` courant.  
    - Mettre à jour `status` et `updatedAt`.  
    - Retourner la candidature mise à jour (ou une vue adaptée).  
- **candidature.delete** :
  - Input : `{ candidatureId: z.uuid() }`.  
  - Comportement :
    - Vérifier le `companyId`.  
    - Supprimer la candidature correspondante.  
    - Retourner `{ success: true }` ou équivalent.

### UI & Components

- **OfferCandidatesSection** :
  - Devient le point central de la gestion du pipeline de candidatures par offre : dropdown statut, bouton Dissocier, filtre par statut.  
  - Penser l’UX de façon à rester lisible avec plusieurs dizaines de candidatures (pagination ou scrolling, mais cela reste optionnel pour le MVP).  
- **CandidateCandidaturesSection** :
  - Doit refléter fidèlement les statuts courants par offre associée.  
  - Optionnel : permettre aussi le changement de statut ou la dissociation depuis la fiche candidat, en veillant à ne pas multiplier des UX contradictoires.

### Testing

- Prioriser les scénarios critiques :
  - Changement de statut en chaîne (ex. "Contacté" → "Postulé" → "Accepté") sans erreurs.  
  - Cohérence des compteurs par statut et du filtrage après changements multiples.  
  - Dissociation correcte (ligne supprimée, compteurs mis à jour, visibilité côté fiche candidat).  
- Vérifier l’accessibilité des menus et dialogues (navigation clavier, focus visible).

---

## Change Log

| Date       | Version | Description                                          | Author |
|------------|---------|------------------------------------------------------|--------|
| 2026-03-11 | 0.1     | Story draft created (3.8 Candidature Status Management) | Bob (SM) ou équivalent |

---

## Dev Agent Record

### Agent Model Used

(Générer / compléter lors de l’implémentation réelle de la story.)

### Debug Log References

*(Aucune pour le draft.)*

### Completion Notes List

*(À compléter par l’agent de dev lors de la mise en œuvre.)*

### File List

*(À compléter par l’agent de dev lors de la mise en œuvre : routers tRPC, composants, tests, docs QA, etc.)*

---

## QA Results

*(À remplir par l’agent QA après review de la story implémentée.)*

